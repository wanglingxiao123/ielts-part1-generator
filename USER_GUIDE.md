# 雅思听力 Part 1 材料生成系统 · 使用指南

这个系统帮你批量生成雅思听力 Part 1 的完整文字套件。你勾选场景，它会生成符合规范的对话脚本、
十个信息点，以及两组共十道题、答案和证据。材料和题目都会经过程序校验及独立 AI 审核，最后仍由你
在页面中审阅、对题目提出修改意见和选稿。

打开地址后先登录。没有账号就在登录页点「注册」，用邮箱和密码（至少 8 位）即可。

> 📷 **截图占位：登录页**

---

## 一、整体流程

```
选场景 → 生成材料与题目 → 审阅原文/题目 →（评价/修改/试听）→ 选稿 → 提交审核
```

顶部三个页签对应这条路的三个阶段，**任何时候都能点**：

| 页签 | 干什么 |
| --- | --- |
| **场景选择** | 勾场景、定数量、提交生成 |
| **生成结果** | 看这一批出来的材料，选稿 |
| **审核队列** | 已提交的材料清单 |

第一次使用时点「生成结果」会看到「还没有生成过材料」，这是正常的。以后再点，它会直接打开你**最近
那一批**。

---

## 二、选场景并提交生成

> 📷 **截图占位：场景选择页，展开的分类与勾选状态**

1. 场景按大类折叠展示（预订/咨询服务、住宿、求职、客服/事务……）。点场景名即勾选，再点取消。
2. 每个勾选的场景可以单独设「生成几套」。**同一场景生成 2 套以上**，才能用后面的「对比」功能。
3. 想要目录里没有的话题，用最下面的「自定义场景」，直接写中文即可，例如「餐厅点餐」。
4. 底部会显示预计耗时。点「提交生成」。

**关于数量**：没有固定的产品上限。系统同时最多跑 6 套，后面的套数排队开始。每套现在包含材料审核
和题目审核，复杂套件可能需要十几分钟；请以结果页的实时进度为准，不要按旧的 3-5 分钟估算。

**关于自定义场景**：你输入的名字会一直跟着这批材料显示，不会变成英文或代码。

---

## 三、生成过程与结果页

提交后**直接进入结果页**，不用等待页。你会立刻看到全部卡位铺成灰色骨架卡（写着「生成中…」），
每套材料和十道题全部完成后，才会把自己的骨架卡换成真卡。

> 📷 **截图占位：结果页，部分骨架卡 + 部分已完成卡片**

页面顶部有整批进度：进度条和「已完成 M/N」。单套内部会依次经过材料生成、校验、盲审、必要的
修改与复评，再进入题目生成、校验、盲审和必要的修题。已经完整交付的卡片随时可以点开看，不用等
整批跑完。

**离开页面没关系**——生成在服务器上继续。回来点「生成结果」还能看到。

如果最终只有部分卡位完成，已完成的套件仍可正常使用。点击「补生成这一套」会在**当前批次的原位置**
继续补齐；成功后计数从 `8/9` 变为 `9/9`，失败提示自动消失，不会另开一个结果面板。

### 卡片上有什么

> 📷 **截图占位：单张材料卡片特写**

| 元素 | 含义 |
| --- | --- |
| **第 N 套** | 这个场景下的第几套 |
| **待审核** | 所有材料都是这个状态，等你选稿 |
| **右上角圆圈** | 勾选框。点一下选中这套 |
| **信息点分布图** | 十个信息点在对话里的位置，见下 |
| **一句引文 + 一行简述** | 这套材料讲什么，用来快速区分 |
| **阅读全文** | 打开完整对话和标注 |

### 信息点分布图怎么看

这是最有用的一张图。横轴是**对话轮次**，圆点是十个信息点的实际位置。

> 📷 **截图占位：分布图特写，含一个黄点和一处密集区**

- **蓝色圆点**：正常的信息点。点上的数字是题号。
- **黄色圆点**：值得先看一眼的点。三种情况会变黄——几个点挤在相邻几轮里（考生来不及记）、题号
  顺序和出现顺序不一致（不能按 1→10 顺序出题）、或者试听者没能听出这个答案。
- **点挨在一起**：图上就是挨在一起。**这不是画错了**——重叠本身就是要告诉你「原文里这几个点确实
  连着给」。
- **中间的竖线**：前后两组题的分界（Part 1 通常分成 1-5 和 6-10 两组）。
- **黄色横条 + 「⚠ 3 点挤在 turn 8-11」**：这一段信息点过密。

**黄点不等于材料不能用**。它是「先看这里」的指路。

---

## 四、阅读全文

点卡片上的「阅读全文」。

> 📷 **截图占位：阅读页全貌**

标题下有两个页签：**对话原文**和**题目预览**。

对话原文页有四块：

1. **顶部**：完整的分布图（比卡片上那张详细），下面一句结论——「可直接出题」/「建议先改」/
   「须先改」，以及四条检查（题号顺序、记录节奏、全篇覆盖、前后两组题量）。
2. **原文**：逐轮对话。旁白是 speaker1，另两位是对话双方。信息点在原文里**高亮**，带圈号（①②③）。
3. **右侧旁注**：每个信息点一张卡，写着它的类型、答案、难度标签、以及引用的原句。
4. **底部**：考点小结、能出哪些题型、篇幅统计。

题目预览页按真实题型显示 Form、Note 或 Table，而不是把十道题摊成普通问答卡。首次打开保持考生
视角，不显示答案和证据；需要复核时再打开「显示答案和证据」。切换页签不会重新生成题目。

### 批注

- 在**题目预览**中点击某一道题，右侧批注栏会自动关联到 Q1-Q10；
- 添加批注时必须选择「重要 / 一般 / 轻微」；
- 有批注的位置显示紫色边线和数量；
- 点击批注卡会跳回对应题目；
- 批注可以删除，刷新页面后仍然保留。

对话原文的评价入口目前暂时隐藏。你仍可阅读、播放、查看高亮与右侧信息点旁注，但不能在 Turn 上新建
评价。后端保留了原有接口和历史数据，之后恢复入口时不会丢失。

### 标签的意思

**信息点类型**（规范里的八类）：

| 标签 | 考什么 |
| --- | --- |
| 姓名/专名 | 人名、地名、机构名，通常要逐字母拼读 |
| 电话/编号 | 电话号码、邮编、编号 |
| 地址/门牌 | 门牌号、街道名 |
| 金额/价格 | 费用、价格 |
| 日期/时间 | 日期、星期、时刻 |
| 数量/上限 | 人数、间数、上限 |
| 条件/要求 | 需要满足什么、附加要求 |
| 选择/偏好 | 在几个选项里挑一个 |

**难度设计**：

| 标签 | 什么意思 |
| --- | --- |
| **拼读** | 姓名逐字母给出，考生要听写字母 |
| **先说后改** | 先给一个值再改口，考最终有效答案 |
| **同义替换** | 用指代或换一种说法绕开答案原词，考生得自己还原 |
| **干扰** | 有干扰设计，但机制没说明——请对照原文自己确认 |
| **有复述确认** | 对话里复述或确认过，一遍就能听清 |

**要注意的两个**：

- **拼读却没人复述**（黄色）：拼读信息只播一次又没人复述，考生极易听错。
- **听不出来**（在对比页不显示）：试听者没能提取出这个答案。

### 试听

在阅读页点「生成音频」。首次合成需要一两分钟（三个角色逐句合成），完成后页面给出播放器：全文播放、
倍速、片段计数、自动滚动跟随。

> 📷 **截图占位：播放器 + 跟随高亮**

**试听不等于选稿**。听过之后同场景的另一套仍然保留。而且这份音频会一直跟着这套材料——将来正式选用
它时不会重新合成、不重复计费。

历史批次的材料同样可以试听。

---

## 五、通过批注修改题目或材料

题目批注提交后，系统会先判断意见需要改到哪一层，不会直接覆盖当前版本。

| 页面反馈 | 含义 | 下一步 |
| --- | --- | --- |
| **无需修改** | 当前题目或材料已经支持这条意见 | 查看理由和引证，不生成版本 |
| **正在修改题目** | 只需调整某道题的题面、答案或证据 | 系统完成后查看新版本 |
| **需要重新命题** | 要调整题型、题组边界、布局或信息点规划 | 点击「确认重新命题」 |
| **需要修改材料** | 原对话无法支持目标答案或要求 | 点击「确认修改材料」 |

### 局部修改题目

只修改批注所指向的题目。其他题、听力原文和蓝图不会跟着模型输出意外变化。新版本仍会走程序校验和
独立盲审；通过后才会出现在版本列表中。

### 重新命题

适用于 Form 改 Note、重组字段、调整题组边界等需求。确认后系统重新规划蓝图并生成完整十题。若你的
意见只要求改布局，系统会保留原来的十个答案信息点和听力材料；若材料本身无法支持合格题组，页面会
明确提示需要修改材料，而不是悄悄改原文。

### 修改材料

适用于把 `three nights` 改成 `four nights` 等原对话不支持的要求。确认后系统生成配套的新听力稿、
蓝图和完整十题，并重新执行校验和盲审。新材料的音频属于该新版本，需要按页面提示生成。

### 版本与重试

- 新版本生成后**不会自动采用**；先在“材料与题目版本”中查看或试听，再点击采用。
- 切换版本时，材料、蓝图、题目、答案、证据和音频归属会一起切换。
- 修改过程中可以刷新或离开页面，后台任务不会中断；回来后页面会恢复进度或终态。
- 失败后使用页面上的“重新尝试命题/修改材料”。它沿用原意见，但启动新的后台执行，不需要删除批注。
- 同一个按钮重复点击不会创建两个版本。

---

## 六、对比两套材料

同一场景有 2 套以上时，场景标题右侧会出现「对比本场景」。

> 📷 **截图占位：对比模式，两张卡片标着 A / B**

点它进入**对比模式**：

- 该场景的勾选框暂时隐藏，**整张卡片变成点选区**——点第一张标 **A**（蓝框），第二张标 **B**（紫框）。
- 其他场景整块变灰、不可点。
- 底栏换成「退出对比」。
- **你之前的勾选不会丢**，底栏会写明「已勾选的 N 套保留着，退出后恢复」。

选满两张，卡片下方**原地展开**对比预览：两列时间轴 + 一张对照表。**不会跳走页面**。

> 📷 **截图占位：内联对比预览**

想看完整对比再点「打开完整对比 →」。

### 完整对比页

> 📷 **截图占位：完整对比页，两栏并排**

顶部一张**对比摘要**，三行：

- **共同点**：两套都考了哪些类型、篇幅是否都合格
- **主要差异**：考点类型、干扰机制的数量差、篇幅、前后两组题量
- **怎么选**：一句建议，例如「要更难就用材料 B（干扰 3 处）；两套都合规，按学生水平挑」

下面两栏并排，每侧是：话题简述、篇幅、前后两组分配、干扰机制计数、信息点类型、原文+时间轴、能出
哪些题型。两栏严格对齐，同一行比的是同一件事。

**「展开旁注」**：默认折叠（只显示高亮和圈号）。点一下两侧同时展开，每个信息点的类型、答案、标签、
引用原文都出现在右侧。再点收回。

对比页**不做选稿**——选稿在结果页勾选。左上角「← 返回批次」回到卡片列表。

---

## 七、选稿并提交审核

> 📷 **截图占位：底栏，已选 N 套 + 提交审核**

在结果页勾选要用的材料。底栏会显示「已选择 N 套材料」。

**规则是每个有材料的场景至少选 1 套**。差哪个场景，底栏会写出来（「还差：酒店预订」）。选齐了
「提交审核」才能点。

提交后跳到「审核队列」，能看到刚提交的材料清单。**不会弹确认框**。

---

## 八、查看历史

结果页左侧是历史批次面板（可以折叠）。

> 📷 **截图占位：左侧历史面板**

每条记录显示：批次编号、时间、场景与套数、状态。可以按场景名搜索、按状态筛选。

**三种状态**：

| 状态 | 含义 |
| --- | --- |
| **待选稿** | 还没提交，可以继续选 |
| **已提交** | 已提交审核，**只读**——可看、可试听，不能改选稿 |
| **已归档** | 只读 |

只读批次的底栏会换成一句说明，而不是把「提交审核」置灰——一个永远点不了的按钮只会让人一直找原因。

如果某一套在当前运行窗口内没有完成，系统会保存已经完成的阶段，不会把它算作成功。点击
「补生成这一套」会只重跑缺失位置并回填当前批次；已经成功的套件不会重跑。若页面明确说明是系统
故障且补生仍失败，请记录批次编号交给运维排查。

---

## 九、常见情况

**为什么我要求 3 套，成功状态一定要看到 3/3？**
系统按交付名额工作：一份候选材料不适合出题时，会在后台重试题目或更换材料。只有完整的
「对话 + 十道题」才算一套成功；少于请求数量不会显示为成功。

**为什么不显示「生成异常」的空卡片？**
空卡片没有可审阅内容，所以不会冒充一套结果。系统会在后台重试题目或更换候选材料；如果最终仍是
系统故障，整批会明确显示未完成或失败，而不会用空卡补足数量。

**卡片上为什么没有分数或等级？**
所有材料统一「待审核」。质量信息通过黄点、考点小结和阅读页的检查结论表达——那些能告诉你**具体
哪里**要看，一个分数不能。

**页面刷新了，正在生成的批次还在吗？**
在。生成在服务器上跑。回到「生成结果」即可，已完成的部分会补齐。

**页面刷新了，正在进行的题目或材料修改还在吗？**
在。修改任务由服务器持有，页面只显示进度。回到同一材料即可恢复当前阶段。

**为什么修改完成后当前题目没有立刻变化？**
系统不会自动采用新版本。请先在版本选择器里检查新版本，再决定是否采用。

**为什么意见被判为“需要修改材料”？**
说明目标答案或要求在当前听力原文中没有依据。只改题面会制造无法从录音得到的答案，因此必须先修改
材料，再重建蓝图和题目。

**「校验意见」是什么？**
阅读页有时会出现一块校验意见，措辞是「材料本身完整可用」加几处「可以看一眼的位置」。它不是错误
清单，材料是完整、可读、可选的。

---

## 十、名词对照

| 界面上的说法 | 指什么 |
| --- | --- |
| 一批 / 批次 | 一次提交生成的全部材料 |
| 一套 / 完整套件 | 一段 Part 1 对话、十个信息点及对应的十道题 |
| 交付名额 | 用户要求的一套结果；后台可为同一个名额更换候选材料 |
| 信息点 | 一个可出题的具体细节，对应一道题（1-10） |
| 轮次 | 对话里的一次发言（不含旁白） |
| 前后两组 | Part 1 惯例的两组题（1-5 / 6-10） |
| 选稿 | 从同场景的几套里挑要用的 |
| 旁注 | 原文右侧解释每个信息点的小卡片 |
| 批注 | 你在题目预览中针对 Q1-Q10 保存的修改意见 |
| 题目版本 | 一套不可变的材料、蓝图、题目、答案和证据快照 |
| 当前采用 | 正式显示和后续修改所基于的版本 |
