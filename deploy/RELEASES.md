# 发布台账

> 本文件仅供部署和运维人员记录版本映射、验证结果与回退依据，不属于产品使用说明。

回退时唯一需要查的表。每次 `runtime.sh` 或 `service.sh` 之后，在这里补一行。

不记录就等于没有回退能力：ECR 镜像标签本身不携带 git commit，三天后没人能凭
`two-states-20260801` 反推出它是从哪个 commit 构建的。

## 当前生产版本

**Runtime 层 `feedback-across-gates-20260813`（commit `29355f3`）+ web 层
`retry-visible-20260813`（commit `0afdfed`）**，分支 `feat/question-revision-versions`，
承载在 Runtime version **53** + ECS taskdef **`ielts-part1-web:68`**。
digest：backend `sha256:eb1c7046fde9e7dfe741da5229b18ea95fa511b728f0aef637d241a200f65189`、
frontend `sha256:7d5ccf628333ba79bb19f982cee12c3b29dc081dd33b7c4144f663e04ffdb563`。**未合并 main**。

✅ **两层的代码终于对齐到相邻 commit。** `29355f3` 只改 `backend/`，其父 `0afdfed` 正是
当前 web 的代码，所以两层各自都是最新——之前 web 曾一路领先到十一个 commit
（`e2c3e19`…`0afdfed` 全都没碰 `backend/`），本轮 Runtime 一次补齐。
**两层的领先方向会来回换，回退前先看这张表确认各层当前 commit，
不要假设某一层总是更新的那个。**
回退时分开指定：Runtime 用 `--runtime-image feedback-across-gates-20260813`，
web 用 `--taskdef 68`（或 `--web-image retry-visible-20260813`）。
上一版 Runtime 是 `quote-anchors-20260813`（commit `4893951`，ver 52，
backend digest `sha256:67201737…7edddb`）；上一版 web 是 `no-span-warn-20260813`
（commit `cb1be02`，taskdef 67，frontend digest `sha256:58e9d42a…5c83bb`）。

⚠️ **`skills/` 只进 backend 镜像（`COPY skills/ /app/backend/skills/`），web 镜像不含它。**
所以改动只碰 `backend/` + `skills/` 时，仅部署 Runtime 就够，不会造成两层 Skill 文件不一致。
反过来 `audio_storage/` 在两个镜像里都有（都在 `/app/audio_storage/`），
改它必须两层都重建——判断该部署哪层时按这个区分，别只看目录名在不在 `backend/` 下。
bundle 本轮未变（仍是 `index-DGoki_ZI.js` / `agentcore-DDHOJWHL.js` / `index-Cw27RNT9.css`），
因为前端未重建。
历史上两条容易误判的经验：`cb1be02` 改了 `styles.css` 但只改注释、被 minifier 剥掉后
css 哈希不变；`e2c3e19` 只改 `web/` 的 python，三个 bundle 文件名与内容完全没变。
**bundle 未换代不等于 web 层没重建**，判断依据是 frontend digest 和 taskdef 号。

⚠️ **CloudFront 上不存在的 `/assets/*` 返回的是 302 到 `/login`，不是 404。** 校验「旧 bundle
已换代」时靠 302 判断是对的，但反过来**不能用 200 判断某个文件还在**——同一次校验里
`index-apwAmWNI.css` 返回 200 恰好是因为它真的还在（css 未变），而三个新 bundle 一度全被
判成 302/DIFFERS，根因是 `docker cp` 把 `assets/` 拷进了已存在的同名目录，
形成 `/tmp/x/assets/`，比对的是上一轮的旧文件名。**`docker cp` 目标目录先 `rm -rf`。**

⚠️ **`web/execution_manager.py` 的执行状态是进程内的（`threading.Condition` + `deque` 环形缓冲），
不是外部存储。** 服务 desiredCount 是 1，所以滚动部署换任务 = 进程换掉 = **所有在飞的生成执行
连同它们的帧缓冲一起丢失**，前端重连也拿不回来（新进程没有那个 execution_id）。
以后在有长任务在跑时重建 web 层，要么等它结束，要么接受这批执行丢失——
这不是本轮引入的回归，而是这个设计的固有前提，记在这里避免下次误判成 bug。

⚠️ **上一轮（`162d596`）出过一次两层错开的事故，记录以免重犯。** 先推 Runtime 到 50 成功，随后
`deploy/service.sh unified-states-20260813` 在镜像构建阶段挂掉：
`MaterialPage.test.tsx(286,7): error TS2741`，`audit_rejection` 少了契约要求的 `code` 字段。
于是 Runtime 已是新代码、web 还停在 59 的旧代码，而 `162d596` 的 backend 新增的
`failure_code: MATERIAL_INFEASIBLE` 与 `REPLAN_*_EXHAUSTED` 只有新 web 才消费
（`web/app.py` 与 `web/question_versions.py` 各一处），**错开期间那条升级为材料修订的状态流转不生效**。
补上 `code` 后重建 web 才恢复一致。教训：**两层同时部署时，先把前端类型检查跑通再推 Runtime。**

❌ **`npx tsc --noEmit` 这个门禁一直是空的——此前每一轮报告里的 "TSC_OK" 都没检查任何文件。**
根 `tsconfig.json` 是 `{"files": [], "references": [tsconfig.app.json, tsconfig.node.json]}`，
而 `tsc --noEmit` **不跟随 project references**（只有 `tsc -b` 会），`--listFiles` 输出 0 行，
所以它恒定退出 0。真正做类型检查的是镜像里的 `npm run build` = `tsc -b && vite build`，
于是类型错误被推迟到 `deploy/service.sh` 构建阶段才爆。vitest 和 oxlint 都不做类型检查，补不上这个洞。
**前端类型门禁请用 `npx tsc -b`（或 `npx tsc -p tsconfig.app.json --noEmit`）。**
自 `d1af40a` 起每轮都已改用 `tsc -b`。

✅ **commit 能复原镜像——连续第二十七轮。** 构建时 `git diff HEAD` 只含 `deploy/RELEASES.md`
（本文件，不进构建上下文），另有未跟踪的 `material/官网听力题型` 与
`docs/images/user-guide/` 下 15 张截图（都不进构建上下文；其中若干是中文文件名，
`USER_GUIDE.md` 引用的是英文名，两边尚未对齐，但与部署无关）。
已逐字节核对：backend 镜像内 `backend/orchestration/manual_material_revision.py`、
`question_loop.py`、`question_revision_plan.py` 与工作树 IDENTICAL（本轮改的就这三个）。

⚠️ **纯前端改动要用「保留下来的字面量」溯源，不要 grep 标识符——它们会被压缩掉。**
本轮想确认 `scriptCommentsOpen` / `turnCommentCounts` 这些局部变量已删，grep 新旧 bundle
都是 **0**，看着像证据其实毫无意义：局部标识符在 minify 后已被重命名，连删除前的 bundle 里
也搜不到。真正能用的是 **CSS class 名和中文文案**这类原样保留的字符串：
`script-comments-toggle` 与 `script-comments-body` 上一代 bundle 各 **1**、新 bundle 各 **0**，
这才证明那段 UI 真的没了（两个 class 在 `src/styles.css` 里仍各留 1 处未清理的规则，
grep 源码时会命中，别误判成没删）。
**注意 backend 镜像里的源码在 `/app/backend/` 而不是 `/app/`**（按 `/app/` 取会报 missing，
那是取错路径，不是镜像不对）；但 `audio_storage/` 在两个镜像里都在 `/app/audio_storage/`，
web 层的 python 源码在 `/app/web/`——**三者根路径不同，逐字节核对时别套用同一个前缀**。

⚠️ **用 grep 验「某段代码已删」时先数父 commit 的基线。** `37e2313` 那轮想确认
`_changed_scope` 里那圈按 group 反向扩大改动范围的循环已删，按 `for number in QUESTION_NUMBERS:`
grep 镜像得到 **2**，看着像没删干净——其实该行在同文件另有两处无关循环，父 commit 是 **3**，
删掉一处正好剩 2。要盯就盯独有的那行（`if any(group_id in changed_groups ...)`，现为 **0**）。

⚠️ **门禁自 `2061a97` 起是六项，多了 `audio_storage/tests/run_tests.py`。** 它检查
`version_audio.py` 与 `voice.py` 能解析且不含 `match` 语句（3.10+ 语法）——因为版本音频生成
是新加的运行时路径，语法层面的回归在构建期不会暴露，只会在合成时才 500。跑门禁时别漏掉这一项。

上一版是 `material-retry-20260813`（commit `ceee47e`，Runtime **51** / taskdef **61**，
backend digest `sha256:52f5ea1e…7b362b`、frontend `sha256:298ed853…02a0ab`，两层同 tag）。
再往前是 `unified-states-20260813-web`（commit `d1af40a`，Runtime **50** / taskdef **60**，
frontend digest `sha256:d320bb98…f8a494`，仅 web 层；其 Runtime 侧代码是 `162d596`，
tag `unified-states-20260813`，backend digest `sha256:a1ff8b0d…37348f`）。
再往前是 `escalate-material-20260813`（commit `fb938c6`，Runtime **49** / taskdef **59**，
frontend digest `sha256:5ad2c99b…25a9b0`，仅 web 层）。
再往前是 `anchored-patch-20260813`（commit `37e2313`，Runtime **49** / taskdef **58**，
backend digest `sha256:4d775ffc…7c2a9b`，仅 backend 层）。
再往前是 `material-versions-20260812`（commit `2061a97`，Runtime **48** / taskdef **58**，
backend digest `sha256:d3bc846b…bb9a46`、frontend `sha256:aa066fe7…f2579ca`，两层同 tag）。
再往前是 `replan-authority-20260812`（commit `b7fab18`，Runtime **47** / taskdef **57**，
backend digest `sha256:944f0de5…d99076`，仅 backend 层）。
再往前是 `replan-scope-20260812`（commit `b11b07b`，Runtime **46** / taskdef **57**，
backend digest `sha256:527d7edf…33fb0`、frontend `sha256:835aa15d…b83b3e`，两层同 tag）。
再往前是 `replan-semantic-20260812`（commit `a8e98b3`，Runtime **45** / taskdef **56**，
backend digest `sha256:47e5dd7f…c1034f`，仅 backend 层）。
再往前是 `replan-retry-20260812`（commit `a68be45`，Runtime **44** / taskdef **56**，
backend digest `sha256:8a0e59ee…dbe28b`，仅 backend 层）。
再往前是 `replan-execute-20260812`（commit `de1cff9`，Runtime **43** / taskdef **56**，
backend digest `sha256:64670271…10484f`、frontend `sha256:e8d3d134…dd98e2`，两层同 tag）。
再往前是 `anchor-numbers-20260812`（commit `ba104f9`，Runtime **42** / taskdef **55**，
backend digest `sha256:3d91f439…1adc4e4`，仅 backend 层）。
再往前是 `stream-recovery-20260812`（commit `8041bab`，Runtime **41** / taskdef **55**，
frontend digest `sha256:e44cf3af…00e0e2`，仅 web 层）。
再往前是 `revision-routes-20260812`（commit `2c1913e`，Runtime **41** / taskdef **54**，
backend digest `sha256:2da76001…685d3e`、frontend `sha256:c58c63d8…9020f3`，两层同 tag）。
再往前是 `comment-grey-20260812`（commit `b81f182`，Runtime **40** / taskdef **53**，
frontend digest `sha256:ffb36f3c…80746f`，仅 web 层）。
再往前是 `comment-scope-20260812`（commit `7d91820`，Runtime **40** / taskdef **52**，
frontend digest `sha256:85d970f6…089199a7a`，仅 web 层）。
再往前是 `revision-heartbeat-20260812`（commit `ed7d2cf`，Runtime **40** / taskdef **51**，
frontend digest `sha256:42a7ec4c…377d2b89`，仅 web 层）。
再往前是 `revision-stages-20260811`（commit `5e4e8ec`，Runtime **40** / taskdef **50**，
backend digest `sha256:6d36c8c4…be04125df`、frontend `sha256:49f0bd56…3fd545ab`，两层同 tag）。
再往前是 `question-versions-20260811`（commit `af1d2ed`，Runtime **39** / taskdef **49**，
backend digest `sha256:c981d5f1…b9e27c2e6`、frontend `sha256:323d4578…62b3e7a8`，两层同 tag）。

❌ **在此之前的四轮 commit 不能复原镜像。** `group-size-20260809`（38）、`job-title-20260809`（37）、
`layout-fidelity-20260809`（36）、`peer-layouts-20260809`（35）构建时工作树都有大量未提交改动
（38 那次是 44 处，HEAD 停在 `8060974`，即 `form-table-20260809` 的代码）。这四轮要么补一个归档
commit，要么按镜像标签回退——版本号和 commit 都推不出线上代码。
**digest 是唯一能凭线上信息反查的锚点**：tag 虽然 IMMUTABLE，但 digest 才是 Runtime
`containerUri` 解析后真正拉到的层。

上一版是 `group-size-20260809`（**仅 backend**，Runtime 38 / taskdef 48，
digest `sha256:a2622bc9…8e3ef4f4`；web 层当时停在 `peer-layouts-20260809`）。
再往前是 `job-title-20260809`（Runtime 37 / taskdef 48，digest `sha256:616469d1…90488136`）。
再往前是 `layout-fidelity-20260809`（Runtime 36 / taskdef 48）、
`peer-layouts-20260809`（Runtime 35 / taskdef 48）。再往前是
`form-table-20260809`（Runtime 34 / taskdef 47 未动）、
`multi-col-table-20260809`（`300e95d`，Runtime 33 / taskdef 47，两层同 tag 且 commit 能复原镜像）、
`single-group-20260809`（Runtime 32 / taskdef 46 未动，只部 Runtime；题组偏好改为
先试单一自然题组 + 表格限一个内容列——**后半条已被 v33 取代**）。再往前是
`group-windows-20260809`（Runtime 31 / taskdef 46，题组与旁白窗口解耦）、
`comments-20260809`（Runtime 30 未动 / taskdef 45，单人批注，第一次只部 web 层；
两层不同版，回退时 web 用 `--taskdef 45`、Runtime 用 `--runtime-image carrier-parens-20260809`）、
`carrier-parens-20260809`（Runtime 30 / taskdef 44，括号与空 carrier 规则修复）、
`stream-budget-20260808`（Runtime 29 / taskdef 43，预算改按流式上限算）。往前依次是
`row-header-20260808`（28 / 42）、`rubric-gate-20260808`（27 / 41）、
`note-sections-20260808`（26 / 40）、`carrier-style-20260808`（25 / 39）、
`page-shape-20260808`（24 / 38）、`anchor-fix-20260808`（21 / 35，回滚基线
[`.deploy-baseline-20260808b.md`](.deploy-baseline-20260808b.md)）。

**版本号推断不出线上代码是哪一版**（19 装的是 17 的镜像，见下方第二次演练：DEFAULT 端点无法被
`update-agent-runtime-endpoint` 重新指向，只能用 `update-agent-runtime` 重推镜像，而那必然产生
新版本号）。判定线上代码只能查 `containerUri`：

```bash
aws bedrock-agentcore-control get-agent-runtime \
  --agent-runtime-id ielts_part1_runtime-fA4wkq8nKf --agent-runtime-version 40 \
  --query 'agentRuntimeArtifact.containerConfiguration.containerUri' --output text
# → <aws-account-id>.dkr.ecr.us-east-1.amazonaws.com/ielts-part1-backend:revision-stages-20260811
```

**`deploy/runtime.sh` 只重指向，不构建。** 直接给它一个还没推的 tag 会以
`not found. Push it first` 退出（本轮就撞了一次）。完整的后端发布是两步：

```bash
bash backend/scripts/deploy.sh <aws-account-id>.dkr.ecr.us-east-1.amazonaws.com/ielts-part1-backend <tag>
bash deploy/runtime.sh <tag>
```

回退命令见 [`rollback.sh`](rollback.sh)。**Runtime 分支已于 2026-08-07 修好**：改用
`update-agent-runtime` 重放目标版本记录的整份配置（不再调对 DEFAULT 端点无效的
`update-agent-runtime-endpoint`），顺序改成先 Runtime 后 web，且 web 步失败会把 Runtime 补偿回去。

```bash
bash deploy/rollback.sh --to <tag> --dry-run   # 先看清，什么都不改；顺带预检两层镜像是否还在 ECR
bash deploy/rollback.sh --to <tag>
bash deploy/rollback.sh --runtime-image two-states-20260801   # 直接按镜像标签回退，绕开版本号
```

`--runtime-version 17` 现在读作「回到 v17 记录的那个镜像和配置」，**不是**「让 v17 重新 live」——
后者 AWS 不允许。回退结果由 `containerUri` 和 ECS taskdef 判定，脚本不再用版本号推断代码版本。

## 台账

新到旧。`git tag` 一列是可回退锚点；空表示该次发布没有单独打 tag。

| 日期 | 镜像标签 | git commit | git tag | Runtime ver | taskdef | 说明 |
|---|---|---|---|---|---|---|
| 2026-08-13（20:2x） | `feedback-across-gates-20260813`（**仅 Runtime**；web 停在 `retry-visible-20260813`）<br>backend digest `sha256:eb1c7046…f65189` | **`29355f3`**（分支 `feat/question-revision-versions`，未合并 main） | — | **53** | 68（未动） | **修订反馈跨质量门禁传递，并修掉一处会白费重试次数的 no-progress 误判。** 5 个文件 +113/−13，只改 `backend/`，`web/`、`frontend/`、`skills/` 一行未动，所以只重建 Runtime。两处逻辑：①`manual_material_revision.py` 的 no-progress 判定原先只比材料，导致「材料已改过、本次重试只需改蓝图」（例如按可行性反馈纠正 `answer_category` 语义）被判为无进展，**白扔掉三次机会中的一次且根本没跑验证和出题**；现在改成 `(material 未变 and blueprint 未变) or material == 原始材料`，注释写明了理由。②`question_loop.py` 把 `current.validation.errors` 也传给 `question_revision_plan`，后者新增 `validate_errors` 形参并以 `[validator error] %s` 前缀并入 must-fix（docstring 同步补上 "and validator errors"）——此前验证器错误不进反馈，重试看不到它。**门禁六项全绿**：backend+web **1211 项**（+2，本轮新增用例）、`npx tsc -b` exit 0、vitest **524 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：backend 镜像内三个改动文件与工作树逐字节 IDENTICAL；父 commit `0afdfed` 基线里 `[validator error] ` / `validate_errors` / `current.validation.errors` / `and revised.blueprint == current_blueprint` / `or revised.material == material` **全部为 0**，镜像内分别为 **1 / 2 / 1 / 1 / 1**。Runtime READY 且 ver **53**，web 已核仍为 taskdef **68** / 1-1 COMPLETED，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（20:0x） | `retry-visible-20260813`（**仅 web**；Runtime 停在 `quote-anchors-20260813`）<br>frontend digest `sha256:7d5ccf62…fdb563` | **`0afdfed`**（分支 `feat/question-revision-versions`，未合并 main） | — | 52（未动） | **68** | **修改失败详情过长时不再把重试按钮顶出视野。** 5 个文件 +84/−21，只改 `frontend/src` 与文档，`web/`、`backend/`、`skills/` 一行未动，所以只重建 web。`QuestionVersionControls.tsx` 把原先直接铺开的 `message` + `blockers` 列表包进一个 `.question-revision-failure-details` 容器（带 `aria-label="修改失败详情"` 与 `tabIndex={0}`，键盘可滚动）；`styles.css` 给它加 `max-height: clamp(120px, 32vh, 280px)` + `overflow-y: auto` + `overscroll-behavior: contain` + `scrollbar-gutter: stable`，并补 `:focus-visible` 描边。`QuestionVersionControls.test.tsx` +38 覆盖。**门禁六项全绿**：backend+web **1209 项**、`npx tsc -b` exit 0、vitest **524 项/31 文件**（比上一轮多 1，正是本轮新增的用例）、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**（纯前端）：`question-revision-failure-details` 在上一代 bundle 的 js 与 css 内均为 **0**，新 bundle 内 js **1** / css **1**；`aria-label` 文案 `修改失败详情` 上一代 **0**、新 js **1**；`overscroll-behavior:contain` 与 `scrollbar-gutter:stable` 上一代 **0**、新 css 各 **1**。三个 bundle 镜像↔CloudFront 全部 IDENTICAL，上一代三个（含 css）均已 302。rollout COMPLETED 1/1，Runtime 已核仍为 ver **52** / READY / `quote-anchors-20260813`，`/healthz` 200，taskdef 68 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（19:4x） | `no-span-warn-20260813`（**仅 web**；Runtime 停在 `quote-anchors-20260813`）<br>frontend digest `sha256:58e9d42a…5c83bb` | **`cb1be02`**（分支 `feat/question-revision-versions`，未合并 main） | — | 52（未动） | **67** | **题组轮次跨度不再当作缺陷来报警。** 8 个文件 +17/−75，只改 `frontend/src` 与 `USER_GUIDE.md`，`web/`、`backend/`、`skills/` 一行未动，所以只重建 web。`domain/compare.ts` −29 删掉 `widestGroupSpan()` 与整条 priority-4 对比规则（原先会因某组跨度超 `GROUP_SPAN_WARN` 而在 A/B 对比里判一方更差）；`domain/formGroups.ts` 去掉 `spanWarn` 字段（`thresholds` 形参随之变为未使用的 `_thresholds`）；`QuestionTypePanel.tsx` 移除「隔太远，要跨半篇回忆」标记；`distributionAxis.ts`、`styles.css` 的注释改为「展示该题组覆盖的轮次范围」这类描述性措辞。`USER_GUIDE.md` −15 同步删掉内部实现说明。**门禁六项全绿**：backend+web **1209 项**、`npx tsc -b` exit 0、vitest **523 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**（纯前端）：`隔太远，要跨半篇回忆` 与 `考生要跨半篇回忆才能填完` 在上一代 bundle `index-C49F3ZR0.js` 内各 **1**、新 bundle `index-BRKb4VPp.js` 内各 **0**；`flag-warn` 前后均为 **2**（该 class 另有 9 处无关用途，见 `AudioPlayer.tsx`、`AnnotationCard.tsx` 等），说明删除范围没有波及其它警示标记。两个 js 镜像↔CloudFront IDENTICAL 且上一代已 302；**css 文件名与内容都没变，但 `styles.css` 确实改了——改的是注释，被 minifier 剥掉了**。rollout COMPLETED 1/1，Runtime 已核仍为 ver **52** / READY / `quote-anchors-20260813`，`/healthz` 200，taskdef 67 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（19:2x） | `custom-example-20260813`（**仅 web**；Runtime 停在 `quote-anchors-20260813`）<br>frontend digest `sha256:d446ded3…75a756` | **`5b089a3`**（分支 `feat/question-revision-versions`，未合并 main） | — | 52（未动） | **66** | **自定义场景输入框的示例文案改短。** 相对上一版 web（`050c798`）跨四个 commit +253/−194，但**唯一的代码改动只有一行**：`ScenarioSelectPage.tsx` 的 `placeholder` 从英文长句 `例如：A student phones a bike shop about repairing a bicycle…` 换成 `例如：餐厅点餐`；其余三个 commit 全是文档（`README.md` +90、`USER_GUIDE.md` 重写、新增 `docs/images/user-guide/README.md`），不进镜像。`backend/`、`skills/`、`web/` 一行未动，所以只重建 web。**门禁六项全绿**：backend+web **1209 项**、`npx tsc -b` exit 0、vitest **523 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**（纯前端）：新文案 `例如：餐厅点餐` 在上一代 bundle `index-CMfTMeXo.js` 内 **0**、新 bundle `index-C49F3ZR0.js` 内 **1**；旧文案 `A student phones a bike shop about repairing a bicycle` 上一代 **1**、新 **0**——换字面量的改动前后计数正好对调，是这类改动最干净的证据。三个 bundle 镜像↔CloudFront IDENTICAL，上一代两个 js 已 302。rollout COMPLETED 1/1，Runtime 已核仍为 ver **52** / READY / `quote-anchors-20260813`，`/healthz` 200，taskdef 66 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（19:0x） | `hide-comment-controls-20260813`（**仅 web**；Runtime 停在 `quote-anchors-20260813`）<br>frontend digest `sha256:9d52c984…a8c6c7` | **`050c798`**（分支 `feat/question-revision-versions`，未合并 main） | — | 52（未动） | **65** | **隐去 Script 面板上的批注控件。** 只改 2 个文件 +17/−101（`MaterialPage.tsx` −101 与它的测试），`web/`、`backend/`、`skills/` 一行未动，所以只重建 web。删掉的是 Script 视图里那块可折叠批注面板及其状态（`scriptCommentsOpen` / `turnCommentCounts` / `turnComments` / `onSelectCommentTurn` 与对应的 `script-comments-toggle`、`script-comments-body` 两个 DOM 节点）；批注本身与 `MaterialComments.tsx` 未动。**门禁六项全绿**：backend+web **1209 项**、`npx tsc -b` exit 0、vitest **523 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**（纯前端，无 python 可比对）：class 字面量 `script-comments-toggle` 与 `script-comments-body` 在上一代 bundle `index-CRAkErOL.js` 内各 **1**、新 bundle `index-CMfTMeXo.js` 内各 **0**；未删的 `comment-empty` / `comment-error` / `正在读取批注…` 前后计数不变（分别归属 `MaterialComments.tsx`、`QuestionVersionControls.tsx` 与 `MaterialPage.tsx` 仍保留的部分），说明删的范围没有超出。三个 bundle 镜像↔CloudFront IDENTICAL，上一代两个 js 已 302。rollout COMPLETED 1/1，Runtime 已核仍为 ver **52** / READY / `quote-anchors-20260813`，`/healthz` 200，taskdef 65 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（18:4x） | `isolate-sessions-20260813`（**仅 web**；Runtime 停在 `quote-anchors-20260813`）<br>frontend digest `sha256:d8b9d143…3216e` | **`e2c3e19`**（分支 `feat/question-revision-versions`，未合并 main） | — | 52（未动） | **64** | **修订执行改用独立的 AgentCore session，不再复用用户的会话。** 只改 2 个文件 +10/−7（`web/app.py` 与 `web/tests/test_question_versions.py`），`backend/`、`skills/`、`frontend/` 一行未动，所以只重建 web。`_start_revision_execution()` 去掉 `session_id` 形参，改为内部 `new_session_id("ielts-revision")` 自建；三处调用点相应不再传 `self.session_for(actor)`。代码注释写明了两个理由：**AgentCore session 可能跨部署被钉在创建它的 Runtime 版本上**（所以长修订不该继承用户的热 microVM），以及共用一个 session 会把本应独立的后台执行耦合起来。**门禁六项全绿**：backend+web **1209 项**、`npx tsc -b` exit 0、vitest **523 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：frontend 镜像内 `web/app.py` 与工作树逐字节 IDENTICAL；`new_session_id("ielts-revision")` 与那句 microVM 注释在父 commit `4893951` 基线均为 **0**、镜像内均为 **1**，被删的 `self.session_for(actor))` 基线 **3**、镜像内 **0**。bundle 三个文件名与内容均未变（前端未改），镜像↔CloudFront 全部 IDENTICAL。rollout COMPLETED 1/1，Runtime 已核仍为 ver **52** / READY / `quote-anchors-20260813`，`/healthz` 200，taskdef 64 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（18:2x） | `quote-anchors-20260813`（**仅 Runtime**；web 停在 `refill-slots-20260813`）<br>backend digest `sha256:67201737…7edddb` | **`4893951`**（分支 `feat/question-revision-versions`，未合并 main） | — | **52** | 63（未动） | **盲审引用锚点在唯一命中时归一化 turn index。** 4 个文件 +81/−17，只改 `backend/` 与 `skills/`，`web/`、`frontend/` 一行未动；`skills/` 只进 backend 镜像（`COPY skills/ /app/backend/skills/`），所以只重建 Runtime。`skills/shared/cross_check_questions.py` +33 新增 `normalise_unique_quote_anchors(review, material)`：**只在该引用在 Script 里只有一处位置时**才纠正模型自报的 turn index（docstring 明写 "only when the quote has one script location"），多处命中时不动、留给既有的 `quote_anchor_errors` 判错。`backend/deterministic/question_crosscheck.py` +18 按既有的惰性 `_load()` 模式加转发（新增模块级 `_normalise_unique_quote_anchors` 并把原先一行三元赋值拆成四行），`backend/steps/agent_steps.py` 在盲审后调用一次，`test_question_loop.py` +40/−… 覆盖。**门禁六项全绿**：backend+web **1209 项**、`npx tsc -b` exit 0、vitest **523 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：backend 镜像内三个改动文件与工作树逐字节 IDENTICAL（注意 Skill 在镜像里是 `/app/backend/skills/shared/…`）；`normalise_unique_quote_anchors` 在父 commit `ab63fee` 两文件里基线均为 **0**，镜像内 crosscheck **7** / skill **1**，docstring 那句基线 0、镜像 1。Runtime READY 且 ver **52**，web 已核仍为 taskdef **63** / 镜像 `refill-slots-20260813`、1/1 COMPLETED，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（18:0x） | `refill-slots-20260813`（**仅 web**；Runtime 停在 `material-retry-20260813`）<br>frontend digest `sha256:edfac5c0…3998ae` | **`ab63fee`**（分支 `feat/question-revision-versions`，未合并 main） | — | 51（未动） | **63** | **失败座位在原批次内补位，不再新开批次。** 9 个文件 +646/−119，只改 `web/` 与 `frontend/src`，`backend/` 一行未动，所以只重建 web。`web/batch_history.py`（+240/−…）新增 `reserve_refill()`：按 `(scenario_key, index)` 在源批次内推导出规范的 `slot_id`，校验补位座位确实属于该批次（`refill seat does not belong to source batch`）、座位下标合法、已提交批次拒绝补位（`submitted batches cannot be refilled`）；`web/app.py` +88 接上该路径，`web/tests/test_batch_history.py` +216 覆盖。前端 `endpoints.ts`、`BatchProgressPage.tsx` 改走补位接口，`agentcore.ts` −29 / `agentcoreHistory.test.ts` −32 删掉原先「补位时另建批次」的客户端逻辑。**门禁六项全绿**：`npx tsc -b`（exit 0、无输出）、backend+web **1208 项**、vitest **523 项/31 文件**、oxlint exit 0、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：frontend 镜像内 `web/app.py`、`web/batch_history.py` 与工作树逐字节 IDENTICAL；`def reserve_refill` 与 `refill seat does not belong to source batch` 在父 commit `c6e227c` 基线各为 **0**、镜像内各为 **1**；三个 bundle（`index-CRAkErOL.js` / `agentcore-t8q_8WrA.js` / `index-apwAmWNI.css`）镜像↔CloudFront IDENTICAL，上一代两个 js 已 302。rollout COMPLETED 1/1，Runtime 已核仍为 ver **51** / `material-retry-20260813`，`/healthz` 200，taskdef 63 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未创建任何线上测试数据** |
| 2026-08-13（02:4x） | `detach-observers-20260813`（**仅 web**；Runtime 停在 `material-retry-20260813`）<br>frontend digest `sha256:783cb8b9…1039e6` | **`c6e227c`**（分支 `feat/question-revision-versions`，未合并 main） | — | 51（未动） | **62** | **生成会话与 SSE 观察者解耦：执行归进程所有，断开连接不再杀掉执行。** 15 个文件 +483/−120，只改 `web/` 与 `frontend/src`，`backend/` 一行未动，所以只重建 web。新增 `web/execution_manager.py`（118 行，全新文件）：`Execution` 用 `threading.Condition` + `deque(maxlen)` 环形缓冲存帧并按序号发号，`observe()` 按 cursor 追帧、无新帧时每 15s 吐一个 `: hb` 心跳；`ExecutionManager.start()` 按 `execution_id` 去重（已存在则返回既有执行、第二个返回值 False），producer 跑在 daemon 线程里，结束后按 `_retention_seconds` 惰性 prune。`web/app.py` 相应重构（+181/−…），`batch_history.py`、`question_versions.py` 小幅跟进；前端 `agentcore.ts`（+69/−…）与 `useHistoricalBatch.ts`、`BatchProgressPage.tsx` 适配可重连的流。**门禁六项，类型检查 `npx tsc -b`**（exit 0、无输出）：backend+web **1199 项**、vitest **523 项/31 文件**、oxlint 干净、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：frontend 镜像内 `web/app.py`、`web/execution_manager.py`、`web/batch_history.py`、`web/question_versions.py` 与工作树逐字节 IDENTICAL；三个 bundle（`index-DSeUf8MA.js` / `index-apwAmWNI.css` / `agentcore-C1TdJfPe.js`）镜像↔CloudFront IDENTICAL，上一代两个 js 已 302。Runtime 已核仍为 ver **51** / `material-retry-20260813`，`/healthz` 200，taskdef 62 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**注意上方那条进程内状态的告知：本轮这次滚动部署本身就会丢掉当时在飞的执行。****未创建任何线上测试数据** |
| 2026-08-13（02:0x） | `material-retry-20260813`（**两层同 tag**）<br>backend digest `sha256:52f5ea1e…7b362b`、frontend `sha256:298ed853…02a0ab` | **`ceee47e`**（分支 `feat/question-revision-versions`，未合并 main） | — | **51** | **61** | **材料修订失败后带质量反馈重试，失败的 blockers 实时推给前端。** 9 个文件 +682/−85，两层都改，所以两层同 tag。backend 侧 `manual_material_revision.py`（+399/−…）改成把每次候选的质量检查结论按类别喂回下一次尝试，`agent_steps.py` 的材料修订提示词新增 `feedback` 参数：按 `validation`/`audit`/`cross_check`/`feasibility`/`questions`/`no_progress` 六类分节渲染成一段 "Independent quality-check feedback from earlier candidates"，并明确要求「每条 blocker 都是强制项、不得回退上一轮已修好的项、必须返回完整材料与配套完整蓝图并重算所有证据锚点与 turn index」。前端侧 `questionRevisions.ts` 的 `decodeRevisionFrame` 现在解析 `question_revision_failed` 帧里的 `blockers`（过滤非字符串、trim、去空），`useQuestionVersions.ts` 把它接进状态，所以材料修改实时失败时无需刷新即可看到 blockers。**门禁六项，类型检查用 `npx tsc -b`**（exit 0、无输出）：backend+web **1192 项**、vitest **522 项/31 文件**、oxlint 干净、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：backend 镜像内 `manual_material_revision.py`、`steps/agent_steps.py` 与工作树逐字节 IDENTICAL，镜像内 grep 新反馈类别 `no_progress` 计数 1；三个 bundle（`index-Dq7xLxNr.js` / `index-apwAmWNI.css` / `agentcore-D-fVUjp5.js`）镜像↔CloudFront IDENTICAL，上一代两个 js 已 302（css 未换代，本轮无样式改动）；taskdef 61 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`；rollout COMPLETED 1/1、`/healthz` 200；`main` 仍 `dca32c4`。**按要求先 Runtime（51）后 web（61），未创建任何线上测试数据** |
| 2026-08-13（01:2x） | `unified-states-20260813-web`（**仅 web**，是同批改动的补建）<br>frontend digest `sha256:d320bb98…f8a494` | **`d1af40a`**（仅一行测试夹具修正；运行时逻辑仍是父 commit `162d596`。分支 `feat/question-revision-versions`，未合并 main） | — | 50（未动） | **60** | **补建 web 层，收尾上一格那次失败的两层部署。** `d1af40a` 只改 `frontend/src/features/material-reader/MaterialPage.test.tsx`（+4/−1），把 `audit_rejection` 补成 `{ code: 'VERDICT_FAIL', message: ... }`，满足 `contracts/api.ts:226` 的 `{ code: string; message: string }`。**运行时行为等同 `162d596`**，Runtime 因此不需重推（`d1af40a` 对 `backend/` 零改动），刻意停在 **50**。门禁六项，且**首次用 `npx tsc -b` 做真实类型检查**（exit 0、无输出）：backend+web **1186 项**、vitest **520 项/31 文件**、oxlint 干净、Skill 与 `audio_storage` 均 `all checks passed`。**溯源**：frontend 镜像内 `web/app.py`、`web/question_versions.py` 与工作树逐字节 IDENTICAL，镜像内 `web/app.py` grep `MATERIAL_INFEASIBLE` 计数 1（确认新状态流转已上线）；三个 bundle（`index-PsPpQb1f.js` / `index-apwAmWNI.css` / `agentcore-DRoDdMKy.js`）镜像↔CloudFront IDENTICAL，上一代 `index-Dfsxf4XG.js`、`agentcore-DIF4Y13j.js` 已 302。Runtime 已核仍为 ver **50** / `unified-states-20260813`，`/healthz` 200，taskdef 60 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**未做线上 E2E、未创建测试数据** |
| 2026-08-13（01:0x） | `unified-states-20260813`（**推 Runtime 成功，web 构建失败**）<br>backend digest `sha256:a1ff8b0d…37348f` | **`162d596`**（分支 `feat/question-revision-versions`，未合并 main） | — | **50** | 59（**未能更新**） | **两层部署只成了一半：Runtime 到 50，web 构建挂在类型错误，taskdef 停在 59。** 改动本身是统一批注修订的状态流转，14 个文件 +959/−201，两层都改：backend 侧 `manual_question_replan.py` 把失败收敛成 `REPLAN_*_EXHAUSTED` 系列码并按 `last_failure` 决定是否升级为材料修订（`failure_code: MATERIAL_INFEASIBLE`），`manual_question_revision.py` 同步调整；web 侧新增 `assessment_artifacts()`（三条路由不再各自拼 material/blueprint/package，统一从同一个评估基线取）与 `_revision_action()`（把可用动作 `confirm_replan`/`confirm_material`/`retry_replan`/`retry_material` 连同不可用原因直接投影给前端），`web/app.py` 新增 `needs_material` 状态流转；前端相应改 `MaterialPage.tsx`、`QuestionVersionControls.tsx`、`useQuestionVersions.ts`。**失败原因**：`deploy/service.sh` 里 `npm run build` = `tsc -b && vite build`，报 `MaterialPage.test.tsx(286,7) TS2741`——`audit_rejection: { message: ... }` 少了契约要求的 `code`。**六项门禁当时全绿**（backend+web 1186、"TSC_OK"、vitest 520、oxlint、Skill、audio_storage），因为 `npx tsc --noEmit` 是空门禁（见上方 ❌ 段）。**后果**：Runtime 50 与 web 59 错开约 20 分钟，其间 `MATERIAL_INFEASIBLE`/`REPLAN_*_EXHAUSTED` 无人消费。下一格 `d1af40a` 补建 web 后恢复一致。**该 backend 镜像仍有效在用**（ver 50 至今） |
| 2026-08-13（00:3x） | `escalate-material-20260813`（**仅 web**；Runtime 停在 `anchored-patch-20260813`）<br>frontend digest `sha256:5ad2c99b…25a9b0` | **`fb938c6`**（分支 `feat/question-revision-versions`，未合并 main） | — | 49（未动） | **59** | **重新命题走不通时允许升级为材料修订，同时补上升级来源的合法性校验。** 2 个文件 +267/−1，只改 `web/question_versions.py` 与其测试，`backend/`+`frontend/src` 一行未改，所以只重建 web。原来材料修订的来源判定是「黑名单」——`operation in {"replan_questions","revise_material"}` 一律拒，于是 `replan_questions` 卡在 `needs_material_revision` 后无路可走；现在改成白名单 `operation in {None,"revise_questions","replan_questions"}`（`None` 是历史记录）。放开的同时新增一段校验：`operation == "replan_questions"` 时要求其 `source_request_id` 本身是一条合法的 `replan_source`，且父记录的 `base_version_id` 与本记录一致、父记录的 `comments` 与本记录的 `source_comments` 完全相同，否则 409 `MATERIAL_REVISION_SOURCE_INVALID`——即升级链条不能跨版本或换批注拼接。门禁六项：backend+web **1179 项**、Skill `run_tests.py`、`audio_storage/tests/run_tests.py` 均 `all checks passed`（本轮无前端改动，未跑 tsc/vitest/oxlint）。**溯源**：frontend 镜像内 `web/question_versions.py` 与工作树 IDENTICAL，镜像内 grep 新错误码 `MATERIAL_REVISION_SOURCE_INVALID` 计数 5、旧黑名单行计数 **0**；三个 bundle 与 taskdef 58 镜像逐字节相同且镜像↔CloudFront IDENTICAL（预期：`frontend/src` 未改）。Runtime 已核仍为 ver **49** / `anchored-patch-20260813`，`/healthz` 200，taskdef 59 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`，`main` 仍 `dca32c4`。**本轮按要求未做线上 E2E、未创建任何测试数据** |
| 2026-08-13（00:0x） | `anchored-patch-20260813`（**仅 backend**；web 停在 `material-versions-20260812`）<br>backend digest `sha256:4d775ffc…7c2a9b` | **`37e2313`**（分支 `feat/question-revision-versions`，未合并 main） | — | **49** | 58（未动） | **`question_only` 修订改成「按批注锚点范围的局部补丁」，模型返回的整包不再原样采用。** 2 个文件 +428/−34，只改 `backend/orchestration/manual_question_revision.py` 与其测试，`web/`+`frontend/` 一行未改，所以只重推 Runtime。新增 `_anchored_question_numbers()` 从持久化的批注锚点推出唯一可编辑范围（无有效锚点直接 `ValueError`），`_normalize_question_only_package()` + `_project_numbered_rows()` 把模型结果当补丁：`question_face.groups`/`instructions` 一律还原为基线，`questions`/`answer_key`/`evidence` 三段只在锚点题号上取模型的行、其余逐行还原基线，且锚点题缺行或重号即报错。`_changed_scope` 删掉了那圈「同 group 的题一律算改动」的反向扩大循环。审计归因也改了：`cross_check` 的 `hard_defects` 与 `needs_review` 从整体 blocker 改为按题号 `audit_issue`，非 `agree` 项统一按题号单独上报，shortfall 的比对基数从 `named` 换成 `non_agree_numbers`（原来拿「已单独上报数」当分母，非 agree 却未被单独上报的题会让等式恒不成立）。边界校验新增「Q%d changed its blueprint evidence turn」——此前只校验答案未改、证据 turn 可以漂走。门禁六项：backend+web **1172 项**、Skill `run_tests.py`、`audio_storage/tests/run_tests.py` 均 `all checks passed`（本轮无前端改动，未跑 tsc/vitest/oxlint）。**溯源**：backend 镜像内 `manual_question_revision.py` 与工作树 IDENTICAL，镜像内 grep `_normalize_question_only_package` 计数 2（定义+调用），已删的 group 扩大行 `if any(group_id in changed_groups ...)` 计数 **0**。ECS 已核仍为 taskdef **58** / 单 deployment 1/1，`main` 仍 `dca32c4`。**本轮按要求未做线上 E2E、未创建任何测试数据** |
| 2026-08-12（22:1x） | `material-versions-20260812`（**两层同 tag**）<br>backend digest `sha256:d3bc846b…bb9a46`、frontend `sha256:aa066fe7…f2579ca` | **`2061a97`**（含 `2a873ef`、`39e7ba2`、`2061a97` 三个 commit；分支 `feat/question-revision-versions`，未合并 main） | — | **48** | **58** | **材料修订落成评估版本：编排 + 版本音频隔离（backend）与确认接口、版本读取、材料页版本切换（web）。** 29 个文件 +2562/−80，两层都改，所以两层同 tag 重建。backend 侧新增 `backend/orchestration/manual_material_revision.py`（294 行）执行材料修订编排、`audio_storage/version_audio.py`（339 行）按版本隔离生成音频，并改 `backend/app.py`（+233）、`backend/audio.py`、`backend/steps/agent_steps.py`、`audio_storage/synthesize.py`；web 侧改 `web/app.py`（+168）、`web/question_versions.py`（+171）、`web/comment_store.py`，前端改材料页版本切换与状态展示（`MaterialPage.tsx` +122、`QuestionVersionControls.tsx` +132、`useQuestionVersions.ts`、`useAudioStatus.ts`、`api/endpoints.ts`、`api/agentcore.ts`、`contracts/questionVersions.ts`、`styles.css`）。**门禁六项**：backend+web **1155 项**、`tsc` OK、vitest **513 项/31 文件**、oxlint 干净、Skill `run_tests.py` `all checks passed`、**新增 `audio_storage/tests/run_tests.py` `all checks passed`**（检查 `version_audio.py`/`voice.py` 可解析且无 `match` 语句）。**溯源**：构建时 `git diff HEAD` 只含本文件；backend 镜像内 4 个 backend 文件 + 3 个 `audio_storage/` 文件与工作树逐字节 IDENTICAL，frontend 镜像内 3 个 `web/` 文件 + 2 个 `audio_storage/` 文件同样 IDENTICAL；三个新 bundle 镜像↔CloudFront IDENTICAL，上一代三个 bundle 已 302；taskdef 58 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`；rollout COMPLETED 1/1、`/healthz` 200；`main` 仍 `dca32c4`。**部署顺序按要求先 Runtime（48）后 web（58），本轮未做线上 E2E** |
| 2026-08-12（21:3x） | `replan-authority-20260812`（**仅 backend**；web 停在 `replan-scope-20260812`）<br>backend digest `sha256:944f0de5…d99076` | **`b7fab18`**（分支 `feat/question-revision-versions`，未合并 main） | — | **47** | 57（未动） | **审稿人的显式改造要求是指令而非建议，分类 Agent 不得以「现状更合适」为由回 `no_change`。** 2 个文件 +40/−0，只改 `backend/steps/agent_steps.py` 里 `classify_question_revision` 的提示词与其测试，`web/`+`frontend/` 一行未改，所以只重推 Runtime。新增的 reviewer authority policy 段落规定：若审稿人要求把 X 改成 Y 而现状是 X，即便 X 本身合法、常规甚至更优，也要按需要的改造路由；`no_change` 只在**目标状态已存在**或**该批注是被材料/题面证伪的事实性缺陷主张**时才允许；Form→Note、Note→Table 这类显式版式转换且目标尚不存在的，一律 `replan_questions` + `replan_scope=layout_only`（除非同时要求换信息点）。这修的是上一轮 `replan_scope` 上线后暴露的判据缺口：范围枚举解决了「改多大」，但没约束「该不该改」，模型仍可能以设计偏好压过审稿人的明确指令。门禁：backend+web **1144 项**、Skill `run_tests.py` `all checks passed`（本轮无前端改动，未跑 tsc/vitest/oxlint）。**溯源**：backend 镜像内 `steps/agent_steps.py` 与工作树 IDENTICAL，镜像内 grep `Reviewer authority policy` 计数 **1**（确认新提示词已进镜像）。ECS 已核仍为 taskdef **57** / 单 deployment 1/1，`main` 仍 `dca32c4`。**本轮按要求未做线上实测** |
| 2026-08-12（21:0x） | `replan-scope-20260812`（**两层同 tag**）<br>backend digest `sha256:527d7edf…33fb0`、frontend `sha256:835aa15d…b83b3e` | **`b11b07b`**（分支 `feat/question-revision-versions`，未合并 main） | — | **46** | **57** | **`replan_questions` 分出 `layout_only` / `retarget` 两种范围，`layout_only` 由 Python 强制保住十个信息点。** 10 个文件 +262/−13。分类 Agent 现在对每条 `replan_questions` 额外返回 `replan_scope`（其余结论必须为 `none`，不符即 `ModelCallError`）；重新命题执行器在本批全为 `layout_only` 时逐题比对 `target`/`evidence`/`turn_index`，任一变动就把差异当 feedback 重试，三轮内最后一次仍是 `scope` 失败则直接 `RuntimeError`，不再误甩给材料层。`web/question_versions.py` 对历史记录做了兼容：`replan_scope` 缺失的旧 source 记录一律按 `retarget` 处理，避免部署后已发出的确认改变语义。因为 `frontend/src/contracts/questionVersions.ts` 也加了 `replan_scope?` 字段，所以两层同时重建（该字段是纯类型声明，编译后 bundle 哈希不变，属预期）。门禁：backend+web **1143 项**、`tsc` OK、vitest **505 项**、oxlint 干净、Skill `run_tests.py` `all checks passed`。**溯源**：构建时 `git diff HEAD` 只含本文件；backend 镜像内 `app.py`/`manual_question_replan.py`/`manual_question_revision.py`/`agent_steps.py` 四个文件与工作树逐字节 IDENTICAL（路径 `/app/backend/`）；三个 bundle 镜像↔CloudFront IDENTICAL；taskdef 57 已核 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`；rollout COMPLETED 1/1、`/healthz` 200；`main` 仍为 `dca32c4`。**本轮按要求未做线上实测、未建账号/批注、未采用版本** |
| 2026-08-12（20:2x） | `replan-semantic-20260812`（**仅 backend**；web 停在 `replan-execute-20260812`）<br>backend digest `sha256:47e5dd7f…c1034f` | **`a8e98b3`**（分支 `feat/question-revision-versions`，未合并 main） | — | **45** | 56（未动） | **答案类别语义错一律记为 `category_semantics`，不再被 `feasible` 冲淡。** 2 个文件 +3/−7，只动 `backend/orchestration/manual_question_replan.py` 与其测试，`web/`+`frontend/` 一行未改，所以只重推 Runtime。上一轮在 `category_semantics_ok is False` 时还要看 `feasibility.feasible` 是否为 `True` 才记 `category_semantics`、否则记 `infeasible`；现在无条件记 `category_semantics`。**这修的是上一轮那条 `RuntimeError` 分支的触发条件**：三轮全是类别语义错才抛错、不甩给材料层，但只要其中一轮的 `feasible` 不为 `True`（含缺字段/`None`），该轮就会被记成 `infeasible`，`all(... == "category_semantics")` 随即不成立，于是又退回「需要改材料」的错误归因。门禁：backend+web **1140 项**、Skill `run_tests.py` `all checks passed`。**溯源**：backend 镜像内 `manual_question_replan.py` 与工作树 IDENTICAL，镜像内 grep 已删分支 `feasibility.get("feasible") is True` 计数 **0**（确认真的移除）。ECS 已核仍为 taskdef **56** / 单 deployment 1/1，`/healthz` 200。**本轮按要求未做线上实测** |
| 2026-08-12（20:0x） | `replan-retry-20260812`（**仅 backend**；web 停在 `replan-execute-20260812`）<br>backend digest `sha256:8a0e59ee…dbe28b` | **`a68be45`**（分支 `feat/question-revision-versions`，未合并 main） | — | **44** | 56（未动） | **可行性不过关的新蓝图改为重试，而不是直接判「需要改材料」。** 2 个文件 +352/−21，只动 `backend/orchestration/manual_question_replan.py` 与其测试，`web/`+`frontend/` 一行未改，所以只重推 Runtime。把 feasibility 从循环外挪进 `MAX_PLAN_ATTEMPTS` 循环内：原先只要第一版新蓝图的 `preflight_verdict` 判 `REGENERATE_MATERIAL` 就直接走 `_finish_needs_material` 终态，现在带着 `verdict.reasons` 当 feedback 再规划一轮，只有三轮都不过才终态。新增 `plan_failures` 记录每轮失败原因（`unchanged`/`validation`/`category_semantics`/`infeasible`/`feasibility`），并把 `REGENERATE_MATERIAL` 纳入「可判定」集合（原先它不在 `{PASS, PASS_WITH_JUSTIFICATION}` 里，挪进循环后若不放行会被误当作判不出来而 `RuntimeError`）；若三轮全是 `category_semantics` 失败则抛 `RuntimeError` 而非报「需要改材料」——答案类别语义错是蓝图自身问题，甩给材料层是错误归因。门禁：backend+web **1140 项**（+5，与新增测试一致）、Skill `run_tests.py` `all checks passed`。**溯源**：backend 镜像内 `manual_question_replan.py` 与工作树 IDENTICAL。ECS 已核仍为 taskdef **56** / 单 deployment 1/1，`/healthz` 200。**本轮按要求未做线上实测**（不建账号、不提批注、不采用版本），上一轮 `replan_questions` 端到端实测结论仍适用于未改动的主干路径 |
| 2026-08-12（19:0x） | `replan-execute-20260812`（**两层同 tag**）<br>backend digest `sha256:64670271…10484f`<br>frontend digest `sha256:e8d3d134…dd98e2` | **`de1cff9`**（分支 `feat/question-revision-versions`，未合并 main） | — | **43** | **56** | **`replan_questions` 从"只会分类"变成真能执行——三个未验路由里的第一个落地。** 20 个文件 +1735/−44，横跨 `backend/`+`web/`+`frontend/src/`，两层都重建。**（1）新增 `backend/orchestration/manual_question_replan.py`（352 行）**：`replan_from_comments()` 按 planning → validating → feasibility → generating → auditing → storing 跑完整重命题，最多 `MAX_PLAN_ATTEMPTS=3` 轮重规划（新蓝图与旧蓝图逐字节相同也算失败并带 feedback 重试）；`preflight_verdict` 判 `REGENERATE_MATERIAL` 就走 `_finish_needs_material` 终态，判不出来直接 `RuntimeError`。**（2）材料不可变有三道锁**：canonical JSON 的 `sha256` 存进版本记录，`replan_blueprint()` 返回的 material 与入参不一致即 `ModelCallError`，编排层在重规划后与出题后再各查一次 `_material_bytes`。**（3）新增 `agent_steps.replan_blueprint()`**：material/current-blueprint/comments 三个文件喂给 agent，明确要求重建全部十点与组边界而非打补丁。**（4）新路由 `POST /api/material-question-replans/{material_id}`**：只接 `source_request_id`，由 `replan_source()` 校验原记录确实是 `status=="replan_questions"`、`base_version_id` 仍是当前采用版本、`comment_outcomes` 与 `source_comments` 一一对应且至少含一条 `replan_questions`，再用 `reserve_replan()`（`_replan_execution()` 查重）保证一个源决策只执行一份。**（5）`settle_revision` 加 `from_statuses`**：重命题执行时允许把批注从 `needs_replan` 结算成 `resolved`（原先只收 `open`，否则批注会永久卡在 `needs_replan`）。**（6）版本记录新增 `blueprint` 字段**，`web/app.py` 两处改为优先用版本自带蓝图、材料记录只作旧版本兜底。**（7）前端**：`needs_replan` 卡片从只报信息变成带「确认重新命题」按钮（且该卡片与带重命题源的失败卡片都不可关闭，避免把唯一入口点掉），新增 6 步 `REPLAN_STEPS` 进度条与 planning/feasibility/generating 三个 stage，`runRevision()` 抽出共用流程并用 `revisionInFlightRef` 防重入。门禁：backend+web **1135 项**、vitest **505 项**、`tsc --noEmit` exit 0、oxlint exit 0、Skill `all checks passed`。**溯源**：backend 镜像内 `app.py`/`manual_question_replan.py`/`manual_question_revision.py`/`agent_steps.py` 与 web 镜像内 `app.py`/`comment_store.py`/`question_versions.py` 共 **7 个文件全部与工作树 IDENTICAL**（注意 backend 镜像里的路径是 `/app/backend/`，不是 `/app/`）；生产 `/assets/index-C6Era0lD.js`、`agentcore-CW8FSzn_.js`、`index-DOrJXmq2.css` 三方 IDENTICAL，线上 `index.html` 确认引用的正是这两个新 bundle，上一版三个 bundle 已全部 302。`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 已在 taskdef 56 核对。部署顺序按约定先 Runtime（43）后 web（56）。**线上实测（`replan_questions` 路由端到端，`20260809-booking-hotel-109b4875`）**：先造两条**都被判 `no_change`** 的批注（说 Q4/Q5 取自同一句——分类器查了原文指出实为 turn 12 与 15；说 Q9/Q10 同为 `document` 类别过窄——分类器指出同类别 <3 项不违规），这两次说明分类器不会为凑路由而放水；第三条批注要求把 Q5 价格点从 Form 组移到 Notes 组并重划边界，判 `replan_questions`（源 request `f9414499`），批注落 `needs_replan`。确认执行后 SSE 依次吐出 planning → validating → feasibility → generating → auditing → storing → completed，**449s** 建出版本 `f0a1421a`（V4）：`operation=replan_questions`、`source_request_id` 回指 `f9414499`、`changed_questions` 为 1–10、`created_by` 为操作人邮箱。**材料真的没动**：版本里的 `material_sha256` 与用 `_candidates/` 原材料重算的哈希完全相同，`_candidates/<mid>.json` 部署前后逐字节 IDENTICAL，`_questions/` 的 `LastModified` 仍是 8-09。**蓝图真的重规划了**：Q5 由 `form`/A 变 `note`/B，`completion_layout_coverage` 由 form 1-5 变 form 1-4 + note 5-10，印刷布局 A 组 4 行、B 组新增 `ROOM COST` 小节；`split_after` **刻意仍是 5**（旁白窗口"questions 1 to 5"写在不可变脚本里，能动的只有印刷组边界，所以 B 组 `narrator_window_id` 变 `None`）；十个答案全部不变（脚本不可变，答案本就不该变）；`field_changes` 首次有真实内容（Q5 的 `group_id` A→B、`carrier_before` `£`→`Nightly charge: ` 等）。盲审 `status=PASS`、`cross_check` 10/10 agreed、`validation.ok=true`，仅一条 `medial` 空格位置的 advisory。**批注结算走通了新加的 `from_statuses`**：`needs_replan` → `resolved` 且 `resolved_by_version_id=f0a1421a`（旧逻辑只收 `open`，这里会卡死）。**护栏五连**：同 `source_request_id` 重放 2.5s 返回同一 `version_id` 且 `elapsed_seconds=0`、S3 里仍只有一份执行记录与一个新版本（幂等）；拿 `no_change` 的源 request 去执行 409 `QUESTION_REPLAN_NOT_AVAILABLE`；缺 `source_request_id` 400；不存在的源 404 `QUESTION_REVISION_NOT_FOUND`；未认证 401。**版本自带蓝图的新取数路径也验了**：采用 V4 后再在 V4 上提一条 Q5 批注，返回引证是 `题面：Nightly charge: [Q5]`——V4 的新题面而非原始的 `£[Q5] per night`，证明喂给 Runtime 的蓝图/包确实取自版本记录。**仍未验路由**：`question_only` 与 `revise_material` |
| 2026-08-12（17:3x） | `anchor-numbers-20260812`（**仅 backend**；web 停在 `stream-recovery-20260812`）<br>backend digest `sha256:3d91f439…1adc4e4` | **`ba104f9`**（分支 `feat/question-revision-versions`，未合并 main） | — | **42** | 55（未动） | **题号改由批注锚点推导，消掉一类假失败。** 2 个文件 +102/−8，只动 `backend/steps/agent_steps.py` 与其测试，`web/`+`frontend/` 一行未改，所以只重推 Runtime。`classify_question_revision` 里 `number = anchored_numbers.get(comment_id, model_number)`——锚点有题号就用锚点，模型报的 `question_number` 只在无锚点时兜底，同时删掉原先"模型题号 ≠ 锚点题号就 `ModelCallError`"那条校验分支。提示词也同步改为「Runtime derives question_number from question-anchored comments」。**这修的是一类假失败**：模型把题号写错一位，整批分类就以 `ModelCallError` 告吹（此前实测的 `49cbe934` 即此类）。**注意生产路径上 `model_number` 实际不可达**：`web/app.py:614` 的修订快照只收 `anchor.type=="question"` 的批注，所以送进分类器的每条批注必然有锚点题号——净效果是题号完全由锚点决定，模型报什么都不影响。门禁：backend+web **1119 项**（+9，与新增测试一致）、Skill `run_tests.py` `all checks passed`；前端未改但仍复跑 `tsc --noEmit` exit 0 / vitest **497 项**确认无牵连。**溯源**：backend 镜像内 `agent_steps.py` 与工作树 IDENTICAL，`manual_question_revision.py`（本轮未改）也 IDENTICAL，镜像内 grep 旧校验分支 `number != anchored_numbers` 计数 **0**（确认真的移除）。ECS 已核仍为 taskdef **55** / 单 deployment 1/1，`/healthz` 200。**线上实测（Runtime 42，`20260809-booking-hotel-109b4875`）**：① 单条 Q10 批注 → `no_change`，`question_number=10` 与锚点一致，引证 `题面：Reference: [Q10]` / `标准答案：HM72` / `材料证据（Turn 30）`。② **锚点优先的干净判定**——同批提交锚在 Q1 与 Q6 的两条批注，但文本刻意分别讨论 Q7 与 Q2（第二条甚至先谈 Q2 才谈 Q6）；返回的 `question_number` 仍严格是 **1** 和 **6**，引证也对应 Q1 `Morgan` / Q6 `Breakfast`。旧代码在这里会因模型报 7/2 而整批失败。两条批注同归 request `bdace976`，各带 3 条引证，`versions` 仍 2 个、`active` 仍 `original` |
| 2026-08-12（16:4x） | `stream-recovery-20260812`（**仅 frontend/web**；Runtime 停在 `revision-routes-20260812`）<br>frontend digest `sha256:e44cf3af…00e0e2` | **`8041bab`**（分支 `feat/question-revision-versions`，未合并 main） | — | 41（未动） | **55** | **SSE 断流不再等于修订失败。** 2 个文件 +193/−14，只动 `useQuestionVersions.ts` 与其测试。流失败/异常后不再直接把 UI 判为失败，而是走新的 `recoverDurableResult()`：按 `[0,250,750]ms` 三次去 `GET /api/material-question-versions` 拉持久化终态——读到终态就 `load()` 展示真实结果，读到 `running` 就保持 queued，只有三次都查不到（`missing`）才显示失败；并用 `revisionAttemptRef` 做代次防护，旧一轮的恢复结果不会覆盖新一轮（`finally` 里也只在代次匹配时才清 stage）。这对应已实测的事实：Runtime 独占终态转移，流断了不代表修订失败。门禁：frontend **497 项**、backend+web **1110 项**、`tsc --noEmit` exit 0、oxlint exit 0。**溯源**：`web/app.py`/`comment_store.py`（本轮未改）镜像内与工作树 IDENTICAL；生产 `/assets/index-Dv-jSGjE.js`（`dc055905f8db9627…`）、`agentcore-BePZmz1N.js`、`index-DoAw5VuA.css` 三方 IDENTICAL，**CSS 哈希这次没变**（仍 `index-DoAw5VuA.css`，与只改 `.ts` 一致），上一版 `index-B4Nt_Te1.js` 已 302；线上 bundle 内确认含 `[0,250,750]` 延迟数组。Runtime 已核仍 41。**线上实测（真实断流）**：提交 Q8 批注后在第 12s 硬断 SSE（`curl: (28)`，`__http=000`，0 字节）；断流瞬间读到 `revision_request=running/21baf323/analysing` + `running_request=running/21baf323` → 命中 `running` 分支（保持 queued，不报失败，旧代码此处会直接判「题目没有修改成功」）；40s 后 Runtime 独立跑完，`revision_request=no_change/21baf323`、`running_request` 清空 → 命中 `terminal` 分支。批注正确落 `no_change` + `decision_reason` + 3 条 Runtime 生成的引证（`题面：On the lower floor: [Q8]` / `标准答案：pool` / `材料证据（Turn 24）`），`active` 仍 `original`、版本数仍 3 |
| 2026-08-12（15:2x） | `revision-routes-20260812`（**两层同 tag**）<br>backend digest `sha256:2da76001…685d3e`<br>frontend digest `sha256:c58c63d8…9020f3` | **`2c1913e`**（分支 `feat/question-revision-versions`，未合并 main） | — | **41** | **54** | **修订前置四路分类，批注按各自结论分别结算。** 21 个文件 +1290/−75，横跨 `backend/`+`web/`+`frontend/src/`，两层都重建。**（1）新增 `classify_question_revision`**：先分类不改稿，每条批注独立判 `question_only`/`no_change`/`replan_questions`/`revise_material`，按 priority（`no_change`<`question_only`<`replan_questions`<`revise_material`）取最高者作整批路由；返回里出现 `package` 等产物即 `ModelCallError`。`revise_questions_from_comments` 降级为只处理已判定 question-only 的那几条。**（2）越权护栏**：`_question_only_boundary_errors()` 在审核之前就拦住改组/改布局/改蓝图答案目标，另加"改完与原包逐字节相同"的反向检查。**（3）`_with_package_references()`**：`no_change` 的引证由 Runtime 从落盘包生成（题面/标准答案/材料证据三条中文模板），**模型自报的 references 一律丢弃**。**（4）`_field_changes()`** 落进新版本供 UI 展示字段级差异。**（5）web 层**：`_reconcile_question_comments` 改为按 `comment_outcomes` 分组、对每组分别 `settle_revision`，批注状态扩到 `open`/`resolved`/`no_change`/`needs_replan`/`needs_material` 并新增 `decision_reason`/`decision_references`；旧记录走 `reasons` 兜底映射。门禁：backend+web **1110 项**、vitest **495 项**、`tsc --noEmit` exit 0、oxlint exit 0、Skill `all checks passed`。**溯源**：backend 镜像内 `manual_question_revision.py`/`agent_steps.py`、web 镜像内 `app.py`/`comment_store.py` 四个文件全部与工作树 IDENTICAL；生产 `/assets/index-B4Nt_Te1.js`、`agentcore-BB2Xi5ac.js`、`index-DoAw5VuA.css` 三方 IDENTICAL。部署顺序按约定先 Runtime 后 web。`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 已核。**线上实测（`no_change` 路由端到端）**：在 Q3 造一条站不住脚的批注（称题面缺 `nights` 单位，实际 `carrier_after` 就是 ` nights`），SSE 20.6s 返回新事件 `question_revision_no_change`（`6b76d408`）；引证确为 Runtime 生成的中文模板（`题面：[Q3] nights` / `标准答案：three` / `材料证据（Turn 10）：three nights.`）；批注落 `no_change` + `decision_reason` + 3 条引证、`resolved_by_version_id=None`，`_comments/<mid>.json` 与 `_question_revisions/…json` 双向确认已持久化；`versions/` 无新增、`active` 仍 `original`、旧的 `3a76c913`（`resolved`）未被误动；DELETE 该批注 409 `COMMENT_READ_ONLY`，再拿它提修订 409 `QUESTION_COMMENT_NOT_FOUND`。**未验路由**：`question_only`/`replan_questions`/`revise_material`，以及 `field_changes` 的前端展示（现存版本都建于该字段之前，值为 `None`） |
| 2026-08-12（12:4x） | `comment-grey-20260812`（**仅 frontend/web**；Runtime 停在 `revision-stages-20260811`）<br>frontend digest `sha256:ffb36f3c…80746f` | **`b81f182`**（分支 `feat/question-revision-versions`，未合并 main） | — | 40（未动） | **53** | **修掉上一轮报出的 CSS 类名不匹配。** 2 个文件 +26/−2：`styles.css` 的 `.comment-card.needs-material` 改成 `.comment-card.needs_material`（与 `MaterialComments.tsx` 由 `status` 拼出的 className 对齐），并补一条断言 `needs_material` class + `需修改材料` 标签 + 删除按钮禁用的测试。门禁：backend+web **1094 项**、vitest **486 项**、`tsc --noEmit` exit 0、oxlint exit 0。**溯源**：`web/app.py`/`comment_store.py`（本轮未改）镜像内与工作树、与上一版镜像双向 IDENTICAL；生产 `/assets/index-DDaRlQio.js`、`agentcore-_ETUF6xY.js`、`index-jOfsbU9U.css` 三方 IDENTICAL。**CSS 差异经逐块 diff 确认只有那两行选择器**。**两个 JS 的文件名哈希也变了但内容没变**：字节数与上一版完全相同（419028 / 14850），逐块 diff 后唯一差异是彼此的 import 文件名——Vite 把 chunk 依赖的 CSS 内容计入 chunk hash，改名后互引也跟着改名，不是代码改动。Runtime 已核仍 40 |
| 2026-08-12（12:1x） | `comment-scope-20260812`（**仅 frontend/web**；Runtime 停在 `revision-stages-20260811`）<br>frontend digest `sha256:85d970f6…089199a7a` | **`7d91820`**（分支 `feat/question-revision-versions`，未合并 main） | — | 40（未动） | **52** | **题目批注按版本作用域化，已处理的批注转只读。** 11 个文件 +433/−8，`backend/` 一行未动，所以只部 web 层。**（1）批注多出两个字段**：`comment_store.py` 给 `anchor.type=="question"` 的批注写 `version_id`（默认 `original`，走 `MATERIAL_ID_RE` 校验）与 `status`（`open`/`resolved`/`needs_material`），`_document()` 对历史行按同样默认值投影，所以旧数据不需要迁移脚本；`CommentService` 加 `RLock`，list/create/delete 全部进锁。**（2）只能在当前采用版本上批注**：POST 先读 `question_versions.list()`，`version_id != active_version_id` 就 409 `COMMENT_VERSION_NOT_ACTIVE`；DELETE 对非 `open` 的题目批注 409 `COMMENT_READ_ONLY`（只读≠删除）。**（3）终态修订回填自己的那批批注**：`_reconcile_question_comments()` 挂在 **`GET /api/material-question-versions`**（不是 comments GET，探针顺序要注意）上，把 `revision_request.source_comments` 交给幂等的 `settle_revision()`——它只动 `version_id == base_version_id` 且仍 `open` 的那几条，`completed` 落 `resolved` + `resolved_by_version_id`，`needs_material_revision` 落 `needs_material`。**（4）修订快照过滤**：POST revisions 的候选批注多两个条件（`version_id == base_version_id`、`status == "open"`）；该过滤**排在 `BASE_VERSION_NOT_ACTIVE` 之前**，所以拿历史版本当 base 会得到 `NO_QUESTION_COMMENTS` 而非 base 不活跃。**（5）前端**：`MaterialPage.tsx` 按所选版本过滤批注列表、把 `version_id` 带进 create、终态修订触发一次 `reloadComments()`；`MaterialComments.tsx` 渲染状态标签与置灰卡片、禁用已处理批注的删除按钮；非活跃版本上 composer 整体禁用并显示「历史版本仅供查看」。门禁：backend+web **1094 项**、`tsc --noEmit` exit 0、vitest **30 files 485 tests**、oxlint exit 0、Skill `run_tests.py` `all checks passed`。**溯源**：`web/app.py`（`7e5c6bf97a1052fb…`）与 `web/comment_store.py`（`667a62791afecdea…`）镜像内与工作树 IDENTICAL；生产 `/assets/index-BnUV4aRd.js`（`3c9f45ac092e871c…`）、`index-BbN2mVCI.css`（`c4b5bef3e14f06f7…`）与镜像内、与工作树构建三方 IDENTICAL（**CSS 哈希这次变了**，`index-1mHcauya.css` → `index-BbN2mVCI.css`，与本轮改了 `styles.css` 一致）。Runtime 已核仍为 40 / `containerUri` 指向 `revision-stages-20260811`。`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 显式传入，taskdef 52 已核。`/healthz` 每次轮询 200，rollout COMPLETED 1/1。**线上实测（`20260809-booking-hotel-109b4875`，共 3 个版本 V1 原始/V2 `3c9d923d`/V3 `be01c8fd`，active 仍 V1）**：① 版本过滤——切到 V2 批注列表清空、切回 V1 那条 Q5 批注回来（V1 题面仍带 `£`、V2 已无，确认切的是真版本）；后端侧拿历史 `base_version_id` 提修订得 400 `NO_QUESTION_COMMENTS`。② 只读置灰——`.comment-card` 实测背景 `#f4f5f7`、字色 `#5e6c84`、左边框转灰，删除按钮 `disabled`；DELETE 已处理批注 409 `COMMENT_READ_ONLY`、DELETE `open` 的 200、删完那条已处理的仍在。③ 历史版本禁止新增——历史 `version_id` 409、伪造的 `no-such-version` 也 409（绕不过去）、active `original` 201；turn 锚点批注不受限（201 且 `version_id` 为空）；前端 composer 实测 `class="comment-composer disabled"`、textarea `disabled`、文案「历史版本仅供查看」。④ 已在 Vn 处理——批注 `3a76c913` 在 `be01c8fd` 完成后落 `status=resolved` / `resolved_by_version_id=be01c8fd` / `resolved_at=2026-08-12T03:25:20Z`，**已持久化到 `_comments/<mid>.json`**（不只是响应投影），UI 渲染「Q5 一般 已在 V3 处理」。**⚠️ 一处已知 CSS 类名不匹配（本轮未修）**：`styles.css` 写的是 `.comment-card.needs-material`（连字符），而 `MaterialComments.tsx` 由 `status` 拼出的是 `needs_material`（下划线），线上实测 `needs_material` 卡片背景 `rgb(255,255,255)` 与普通卡片一致、拿不到置灰。`resolved` 不受影响；`QuestionVersionControls.tsx:172` 的 `needs-material` 是修订结果横幅的另一处硬编码字面量，与此无关 |
| 2026-08-12（09:3x） | `revision-heartbeat-20260812`（**仅 frontend/web**；Runtime 停在 `revision-stages-20260811`）<br>frontend digest `sha256:42a7ec4c…377d2b89` | **`ed7d2cf`**（分支 `feat/question-revision-versions`，未合并 main） | — | 40（未动） | **51** | **修掉 409 死锁与 60s 断流，删掉死枚举 `revising`。** 5 个文件 +126/−11，`backend/` 一行未动，所以只部 web 层。**（1）`reserve()` 不再先删指针**：`if_none_match=not pointer_exists`——终态/陈旧指针直接覆盖，缺指针时仍用 create-only PUT 防并发；这解掉了上一轮实测到的永久 409（任务角色刻意不含 `s3:DeleteObject`，而 `DeleteObjects` 把逐键拒绝放在响应里、不必然抛异常，于是 `if_none_match=True` 必撞 `PreconditionFailed`）。请求记录写失败时改为把 `running_key` 落成 `failed` 而非删除，存储不可用时宁可保留 marker 也不放第二个请求进来。**（2）SSE 心跳**：新增 `_iter_revision_payloads()`，用工作线程读阻塞的 Runtime body、主线程按 `HEARTBEAT_SECONDS`（`web/fanout.py`，默认 **15**，可用 `WEB_SSE_HEARTBEAT` 覆盖；taskdef 51 未设即 15）超时就吐 `: hb` 注释帧，`finally` 里关上游并 `join(timeout=1.0)` 回收线程。**（3）** 契约与 UI 删掉从未有人写入的 `revising` 阶段。门禁：backend+web **1087 项**、`tsc --noEmit` exit 0、vitest **30 files 482 tests**、oxlint exit 0、Skill `run_tests.py` `all checks passed`。**溯源**：3 个 web 文件镜像内与工作树 IDENTICAL；生产 `/assets/index-Crwjzrnn.js`、`agentcore-BQMp_0sv.js`、`index-1mHcauya.css` 与镜像内、与工作树构建三方 IDENTICAL（两个 JS 哈希变、CSS 未变，与「只改 tsx/ts 未改 `styles.css`」一致；上一版 `index-GXVLnjP5.js` 已 302）。`COPY frontend/ ./`＋`npm run build` 非缓存。Runtime 已核仍为 40 / `containerUri` 指向 `revision-stages-20260811`。`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 显式传入，taskdef 51 已核。`/healthz` 200 @ 1.06/0.74/0.73s，STABLE 1/1 单 deployment。**线上实测（同一材料 `20260809-booking-hotel-109b4875`，`_question_revisions/…/running.json` 保持上一轮 `completed` 终态、**刻意不删**）**：POST 直接被接受，无 409；SSE 心跳实测 `09:44:42 → :57 → 45:12`、事件后 `45:42 → :57 → 46:12`，相邻间隔 `[15,15,30,15,15]`（那个 30 是中间插了 `started`+`validating` 两个真事件重置了计时器，符合预期）；连接活到 **117.5s**（`__http=200`，远超上一版被掐断的 60s），并完整收到 `started → validating → auditing → failed` 四个事件——上一版这条流只能收到前两个。指针被正常覆盖为新请求 `49cbe934`。**本次修改本身失败在质量门**（`Q5's evidence anchor is one turn from the writer's and unconfirmed`，非本轮改动引入），上一轮的新版本 `3c9d923d` 完好、`active_version_id` 仍 `original`，失败态与 blockers 也能从 API 刷新读回 |
| 2026-08-11（21:1x） | `revision-stages-20260811`（**两层同 tag**）<br>backend digest `sha256:6d36c8c4…be04125df`<br>frontend digest `sha256:49f0bd56…3fd545ab` | **`5e4e8ec`**（分支 `feat/question-revision-versions`，未合并 main）<br>连续第二轮 commit 可复原镜像 | — | **40** | **50** | **修改进度可持久、盲审波动按改动范围分级。** 14 个文件 +772/−31，横跨三层，两层都重建。两处改动：**（1）阶段持久化**——`manual_question_revision.py` 在每一步把 `stage` 写进 `_question_revisions/<mid>/<req>.json` 并同时 SSE（`analysing`/`validating`/`auditing`/新增 `storing`），web 的 `reserve()` 落 `stage:"queued"`，`list()` 新增 `revision_request`（**含终态**）与 `original` 版本的 `quality` 投影，新增 `_latest_request()` 让终态可被读回；**（2）`_revision_gate()`**——以 `base_version` 为基线算 `_changed_scope()`，落在**未改动**题号/题组上的盲审 MAJOR/CRITICAL 与 AR-012 同解答案降级为 `baseline_advisories`，而 cross-check 的 `hard_defects`/`leakage`/`needs_review`、校验器 errors、覆盖面与自相矛盾检查**一律仍然阻断**。门禁：backend+web **1085 项**、frontend `tsc --noEmit` exit 0 / vitest **30 files 482 tests** / oxlint exit 0、Skill `run_tests.py` `all checks passed`。**溯源**：backend 3 个改动文件、web 3 个改动文件镜像内与工作树全部 IDENTICAL；生产 `/assets/index-GXVLnjP5.js`、`index-1mHcauya.css`、`agentcore-wPACgBdi.js` 与镜像内、与工作树 `npm run build` 三方 IDENTICAL（旧 bundle 已 302）；`COPY backend/`＋`COPY skills/`、`COPY frontend/ ./`＋`npm run build` 均非缓存。`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 显式传入，taskdef 50 已核。`/healthz` 200 @ 0.88/0.71/0.68/0.69s，STABLE 1/1 单 deployment。**线上实测（`20260809-booking-hotel-109b4875` 的 Q5 原批注重提）**：阶段字段 `analysing → auditing` 在 S3 与 `GET /api/material-question-versions` 两侧全程同步可见，终态 `completed` 带 `version_id`；新版本 `3c9d923d` 落盘，逐字段比对**只有 Q5 的 `carrier_before` 由 `'£'` 变 `''`**，`canonical` 仍 `£128`，其余 9 题的题面/答案/证据与题号顺序全未动，`review qc=PASS findings=0`，`active_version_id` 仍 `original`（完成≠采用）。`changed_questions=[1,2,3,4,5]` 是**刻意按题组扩展**（Q5 变→其所属组 A 标记已改→组内全员纳入），不是越权改题。Q9 本轮盲审判 `passport` 为 `equally_supported=false`，波动未复现，故用**线上真实新版本包**离线注回该 rival 验证分级：blockers 为空、只产出 `baseline_advisories`（Q9 未改）；反向对照把 Q9 自身改动后同一条 rival 恢复阻断——豁免不泛化。**两个已知问题（本轮未修，见下方遗留）**：`reserve()` 的 409 死锁仍在（任务角色缺 `s3:DeleteObject`）；SSE 在 60s 静默后被掐断，靠前端 5s 轮询兜住 |
| 2026-08-11（20:0x） | `question-versions-20260811`（**两层同 tag**）<br>backend digest `sha256:c981d5f1…b9e27c2e6`<br>frontend digest `sha256:323d4578…62b3e7a8` | **`af1d2ed`**（分支 `feat/question-revision-versions`，未合并 main）<br>**五轮来第一次 commit 能复原镜像** | — | **39** | **49** | **人工复审改题 + 题目版本管理。** 20 个文件 +2288/−41，横跨三层，所以两层都重建。backend：新增 `orchestration/manual_question_revision.py`（+184）与 `tests/test_manual_question_revision.py`（+167），`app.py`/`slot_store.py`/`steps/agent_steps.py` 接入；web：新增 `question_versions.py`（+234）+ 258 行测试，`app.py` +217 挂三条路由；frontend：新增 `api/questionRevisions.ts`、`contracts/questionVersions.ts`、`QuestionVersionControls.tsx`、`useQuestionVersions.ts` 及四个测试文件。三条 API：`GET /api/material-question-versions/{mid}`、`POST /api/material-question-versions/{mid}/{vid}/adopt`、`POST /api/material-question-revisions/{mid}`（后者快照 question 批注后转发一次 Runtime 修订并以 SSE 回传）。门禁：backend+web **1078 项**、frontend `tsc --noEmit` exit 0 / vitest **30 files 479 tests** / oxlint exit 0、Skill `run_tests.py` `all checks passed`。**溯源逐字节做实**：backend 镜像内 af1d2ed 改动的 5 个 Python 文件与工作树全部 IDENTICAL；生产 `/assets/index-Zye6JGGr.js`、`index-Bf7vZc2t.css`、`agentcore-Dyai81c0.js` 与镜像内、与工作树构建产物三方 IDENTICAL（`/static/assets/` 前缀仍 302，说明 200 不是路由假象）。两个 Dockerfile 阶段都确认**不是缓存**：backend 的 `COPY backend/`＋`COPY skills/`、web 的 `COPY frontend/ ./`＋`npm run build`（`tsc -b && vite build`）都实际执行。**API 验证靠响应形状而非状态码**：未认证时中间件在路由解析前短路，连不存在的路径也返回 401，所以 401 证明不了路由存在；注册探针账号后打真请求——API 1 返回 `active_version_id`+`versions[]` 完整 package，API 2 幂等采用 `original` 得 200、不存在的 version 得 404 `QUESTION_VERSION_NOT_FOUND`，API 3 缺 `base_version_id` 得 400、不存在的 comment_id 得 **409 `QUESTION_COMMENT_NOT_FOUND`**（这条最有说服力：它证明 handler 走完了「读评论→比对 requested_ids」这段新代码，而非仅路由存在）。对照组：不存在的 `/api/…-nope/` 带 session 会落到 SPA catch-all 返回 HTML，与三条 API 的 JSON 判然有别。`/healthz` 200 @ 0.91/0.67/0.67/0.66s，service STABLE 1/1 单 deployment。**`ALLOWED_EMAIL_DOMAINS` 本轮是五轮来第一次重新生效**（前四轮未动 ECS）：值 `amazon.com,example.com` 不靠记忆，从生产 taskdef 48 实读后显式传入，taskdef 49 已核，并在生产实测 `gmail.com` 注册被 **403 `EMAIL_DOMAIN_NOT_ALLOWED`** 拒绝——没有退化成 `*`。**构建期故障**：Docker Desktop 中途强制登录，两个 buildx builder 全挂（连 `docker ps` 都返回 Sign in），web 构建失败一次；backend 那次已推成功且日志确认非缓存。恢复后重推，web 的同名 tag 因首次失败在推送前中止而仍可用（IMMUTABLE 仓库已核为 free）。未运行实际生成批次（由人在前端验证） |
| 2026-08-09（夜 22:3x） | `group-size-20260809`（**仅 backend**；frontend 停在 `peer-layouts-20260809`）<br>digest `sha256:a2622bc9…8e3ef4f4` | **无（工作树 44 处未提交，HEAD `8060974`）** | — | **38** | 48（未动） | **题组通常至少 3 题，1–2 题碎组要有独立理由。** 纯散文规则轮：材料生成 SKILL、材料 specification、出题 SKILL、题目审核 SKILL、审核 rules 第 15 项五处同轮写入「Groups should normally contain at least three questions … unless it represents a genuinely independent information structure that cannot naturally join either adjacent group」。审核 rules 第 15 项把它列为 SC-015 finding，第 13 项补入稀疏对角矩阵的占位符判据（`—`/`-`/`N/A`/空串，「只适用于一行的维度不是共享比较轴」），新增第 16 项「伪 Form 或被压制的 Note」；出题 SKILL 把第 2 步从「选择题组」改写为「渲染蓝图题组」并明确三种 layout 的正面判据（form=一条真实记录的字段；note=需求/偏好/流程/设施/建议/理由/安排等主题化说明点；table=重复实体在共享维度上比较），删掉 note 的 fallback 地位。**Python 侧无对应硬校验**——这一层是 Agent 语义判断，`run_tests.py` 加的是**散文断言**：对四个文件各查三个短语（`at least three` / `genuinely independent` / `cannot naturally join`），共 12 条新用例。**`question-rules.md` 与可行性 SKILL 有意不含这条**：前者只讲「组数由蓝图决定、出题阶段不得增删移动边界」，≥3 属规划期判断；后者是**已知缺口**，见下方遗留。门禁：镜像内 `run_tests.py` **610 项全过 exit=0**（含上述 12 条），backend+web 1063 项。**验证手段本轮换过一次**：`docker run` 一度全部卡在 `Created`（daemon API 正常、`docker ps`/`system df` 秒回，但容器起不来），改用 `docker create` + `docker cp` 把 `/app/backend/skills` 整棵取出比对——**镜像内 25 个 skill 文件与工作树逐字节一致**，差异只有 `.DS_Store`/`__pycache__`/`tests/`/zip（本就不入镜像）；随后容器层恢复，又在真容器内复跑了一遍 610 项确认。**只部 Runtime**：改动全是 skills 下的 5 个 md + `run_tests.py`，`frontend/src` 与 `backend/` Python 均未动。Runtime READY，`containerUri` 已核为新镜像，`/healthz` 200 @ 1.00/0.84/0.68s，web 层确认未动（taskdef 48、running 1/1、单 deployment）。未运行实际生成测试（由人在前端验证）。**遗留**：可行性 SKILL 是唯一能在材料落盘前拦住 1–2 题碎组的关卡，但它没有这条表述——上一轮刚给它加了「在蓝图阶段拒绝不自然的组边界」，措辞里不含题数下限，所以碎组只会在出题/审核阶段被发现，那时已无法改题组规划 |
| 2026-08-09（夜 20:5x） | `job-title-20260809`（**仅 backend**；frontend 停在 `peer-layouts-20260809`）<br>digest `sha256:616469d1…90488136` | **无（工作树 43 处未提交，HEAD `8060974`）** | — | **37** | 48（未动） | **`answer_category` 加第 14 类 `job_title`，补上「职位名无处可归」的结构性缺口。** 上一批（`web-1786275974185-2`，Runtime 36 首批）只交付 5/6，slot-3 这个位置连开三代（`slot-1` → `slot-1r1` → `slot-1r2`）、每代烧 3 份候选，正好撞满 `MAX_REPLACEMENT_SLOTS`/`MAX_CANDIDATE_SWAPS` 的 9 份上限；**九次失败全是 `material/feasibility_regenerate` 且全都指向 `answer_category` 语义，零次涉及 layout**。根因是 employment-vacancy 把职位名（`warehouse assistant`）选作答案点，而 13 个枚举值里没有职业/角色，rubric 又明写「There is no catch-all」——换多少份材料都必拒，与 `9 and 1` 装不进任何 rubric 同性质。本轮 `ANSWER_CATEGORIES` 13→**14**（`job_title` 排在 `quantity` 之后），read/write 侧蓝图 schema、`question_package.schema.json` 的 enum 同步 13→14，`feasibility.schema.json` 描述文本改为「14 permitted strings」；`answer-category-decisions.json` 新增 `named_occupation_or_role` 规则并**插在 order 2**，其余规则依次后移（artefact→3、contact→4、location→5、service/facility→6、preference→7、no_catch_all→8），`$comment` 的「seven」改「eight」。**插在 2 而不是追加在末尾是关键**：职位名同时像 facility（工作地点）也像 service（所做的事），排序若在 `performed_or_merely_present`（order 6）之后就仍会被判成 facility——缺的是顺序不是措辞。**镜像内验证**：`ANSWER_CATEGORIES` n=14 且含 `job_title`、仍无 `other`；两处 schema 的 `answer_category` enum 各 n=14 含 `job_title`；决策表 14 类与 read-side enum **完全同序**；`procedure` 的 order 连续 1..8 无重复，`job_title` 落在 order 2 且 facility/service 规则严格排在它之后；13 条 `cases` 的 `expect` 全在 14 类内，其中 `warehouse assistant` 一例 `category=job_title` / `not=facility`。**新旧镜像同一输入差分**：把真实生产蓝图 `20260809-employment-vacancy-b5b8eb21` 的 Q6 改成 `answer_category=job_title`，在 `layout-fidelity`（v36）下报 `blueprint.items[5].answer_category 'job_title' is not in the taxonomy [...]` 1 错，在本轮 v37 下 `ok=True` 0 错——正是上一批走不通的那条路现在通了。另 `shasum` 逐字节比对 9 个相关 skill 文件（校验器 ×2、schema ×4、决策表、可行性 SKILL、审核规则），镜像内与工作树完全一致。**只部 Runtime**：`job_title` 只出现在 skills/schema 与前端 `contracts/*.ts`（由 schema 重新生成的类型联合），grep 确认 `contracts/` 与 `mocks/fixtures/` 之外没有任何运行期 `answer_category` 映射，前端渲染不需要重建。门禁：Skill 基线**全过**（含 `the table's 14 values are the schema's enum, in the same order`、四条 `... accepts job_title`、`'warehouse assistant' -> job_title (rule 2, not facility)`、`'BT14 9BJ' -> location (rule 5, not contact)`）、backend+web **1063 项**、`tsc --noEmit` exit 0。Runtime READY，`containerUri` 已核为新镜像，`/healthz` 200 @ 0.94/0.65/0.70s，web 层确认未动（taskdef 48、running 1/1、单 deployment）。未运行实际生成测试（由人在前端验证）。**遗留**：可行性的拒绝理由不进 CloudWatch（五批 filter 全 0 命中），只落在 slot 文件的 `last_failure.detail.reasons` 里，所以「本轮是否降低了拒绝率」只能靠下一批 employment-vacancy 的实测对比，无法事后复盘 |
| 2026-08-09（夜 19:3x） | `layout-fidelity-20260809`（**仅 backend**；frontend 停在 `peer-layouts-20260809`） | **无（工作树 41 处未提交，HEAD `8060974`）** | — | **36** | 48（未动） | **题目阶段不再重新规划题组，只渲染蓝图结构。** 新增 `validate_layout_fidelity`（`validate_questions_part1.py`，在 `if items:` 守卫下与 `validate_blueprint_fidelity` 并列调用），三条硬检查：印刷组的 `layout` 必须等于其每个成员点的蓝图 `item_form`；一个蓝图 `form_group` 不得拆到多个印刷组；多个蓝图 `form_group` 不得并进一个印刷组。同轮把 Form/Note/Table 改为平级语义判定（`note` 不再是 fallback——出题 SKILL/rules、材料 SKILL/spec、材料审核 SKILL/rubric、可行性 SKILL/rubric 八处都删掉了「note 是 fallback / 默认」的措辞），题目审核规则新增第 16 项「伪 Form 或被压制的 Note」，第 13 项补入稀疏对角矩阵，可行性 SKILL 第 2 步新增在蓝图阶段就拒绝伪 Form / 伪 Table / 不自然组边界。**最强证据是拿真实生产包在三个镜像上跑同一条 CLI**：`20260809-employment-vacancy-191d4462`（Runtime 34 生成、盲审 0 findings、当时 PASS 交付）在 `form-table`（v34）下 **0 错**，在 `peer-layouts`（v35）下 **18 错**（全是 `—` 占位符），在本轮 `layout-fidelity`（v36）下 **20 错**——多出的两条正是本轮新增的：`Q10 is planned as 'table' ... but its printed group 'application_choice' declares layout 'form'` 与 `blueprint form_group 'Vacancy comparison' is split across printed groups ['application_choice', 'vacancy_comparison']`。这个包的蓝图是 2 个 `form_group`，题目阶段却印成了 3 组并把 Q10 从 table 降成 form——**旧三层规则全部放行，本轮才拦住**。同一批的合法包 `20260809-booking-festival-1f80a5a8`（蓝图 1 组 form / 印刷 1 组 form）在新校验下仍 `ok=True` 0 错，无误伤。另在镜像内构造九种判定：layout 一致→0；蓝图 note 印成 form→逐题报错；蓝图 table 印成 note→逐题报错；一组拆两组→split 错；两组并一组→merge 错；两组 1:1→0；纯 note 单组 Q1-Q10→0（平级地位）；`item_form` 缺失或不在 `LAYOUTS`（如 `diagram`）→0（不误伤）。材料侧确认 `TABLE_FORMS` 已删、`ITEM_FORMS={form,note,table}`、1/2/3 组放行而 4/5 组报「1-3 natural candidate-visible Form, Note, or Table groups」。**只部 Runtime**：`skills/` 下 12 个文件的 mtime 晚于 18:33 的 frontend 镜像推送时间，`frontend/src` 无更新——本轮是给生成加约束，前端渲染未变。门禁：Skill 校验器基线全过（含 `blueprint Note points silently reclassified as Form` 与 `blueprint group boundaries replanned by the question stage` 两条新用例）、backend+web 1063 项。Runtime READY，`containerUri` 已核为新镜像，`/healthz` 200 @ 0.95/0.73/0.66s，web 层确认未动（taskdef 48、running 1/1、单 deployment）。未运行实际生成测试（由人在前端验证） |
| 2026-08-09（夜 18:3x） | `peer-layouts-20260809`（backend + frontend 同 tag） | **无（工作树 39 处未提交，HEAD `8060974`）** | — | **35** | **48** | **Form/Note/Table 三者平级，题组限 1–3 组，稀疏对角表被硬拦。** 校验器新增占位符拒绝：`table_rows` 的普通 `{"text"}` 格若整格只是 `—`/`-`/`–`/`N/A`/`n/a`/`Not applicable`/`TBD`/`unknown`，即报 `placeholder-only text ... not padding for a sparse diagonal table`。这条直接针对 v34 那批放行的 7 列对角矩阵（28 格 = 6 空格 + 18 个 `—` + 4 行标签）。schema `groups.maxItems` 与 `instructions.maxItems` 10→**3**；材料侧删掉 `TABLE_FORMS`，`largest` 改按 `ITEM_FORMS`（三种布局）算，新增 `len(labels) > 3` 即报错。前端 `batchEstimate.ts` 的 `WAVE_SECONDS` 由 `[182, 230]` 改为 `[18*60, 40*60]`。**镜像内实跑 11 例**：真实对角表 3 个 `—` 全拦、七种占位符各 1 错、真实两列比较表与三列稠密表 0 错放行、`e-mail`/`co-op` 不误伤。门禁：Skill 基线全过、backend+web 1063、`tsc --noEmit` exit 0、vitest 467/27 文件、oxlint exit 0、`git diff --check` 干净。`services-stable` 2:30，bundle `index-C7mJWqfB.js`、`agentcore-D9zMZr3t.js` 均新（CSS `index-gv5RB1vA.css` 未变），`/assets/<name>` 均 200，旧 `index-bQyZ-yhU.js` 已 302。**注意 bundle 前缀是 `/assets/`，不是 `/static/assets/`**——后者对已存在的 bundle 也返回 302（登录跳转），与「不存在」无法区分，本轮踩过一次 |
| 2026-08-09（下午 16:5x） | `form-table-20260809`（**仅 backend**；frontend 停在 `multi-col-table-20260809`） | `8060974` | — | **34** | 47（未动） | **区分字段表单与比较表格。** 三层规则明确：Form 每行是一个独立字段及其值；Table 至少两个可比较的数据维度；两列表格仍合法但不能只是给 Form 加边框；不为通过规则强行制造第三列。属 Agent 语义判断，Python 侧无对应硬校验，材料生成与题目盲审各查一次。**验收（两批）**：`web-1786265886031-2` 6/6 / 2356s，table **0** 张——读完六套 form 内容确认都是纯字段/值登记表、无可比轴，所以归零正确而非过度纠正；但「真有可比轴的内容仍会被画成 table」这半条**未被考验**。`web-1786268877027-3` 6/6 / 944s 把这半条考出来了并且**失败**：`employment-vacancy-191d4462` 生成 7 列稀疏对角矩阵，盲审 0 findings / PASS，全部硬检查通过。根因是「至少两个可比较维度」被 ≥2 个列标题字面满足，没有要求多行可比观测——这直接导致下一轮（v35）加占位符硬拦、（v36）加 layout 保真 |
| 2026-08-09（下午 16:0x） | `multi-col-table-20260809`（backend + frontend 同 tag） | **`300e95d`** | — | **33** | **47** | **支持真正的多列表格题型。** 上一轮（v32）为了适配前端把表格**限死在一个内容列**，本轮改成让前端支持多列、后端给出精确坐标——**限制被取代而非叠加**。新契约是 `structure.table_rows[]`：按印刷顺序逐行给 `cells[]`，每格**恰好**是固定文字（`{"text": ...}`）或一个题号引用（`{"question_number": N}`），二者不能同时出现也不能都不出现。校验器要求**行矩形**（每行格数 = `len(column_labels)`）且 `table_rows` 引用的题号是该组成员的**精确、升序、不重复**覆盖。**契约方向变了，是最容易踩的一点**：`column_labels` 现在包含**全部**印刷表头（含最左的行标签列），`row_header_label` 与 `row_labels` 降级为旧包只读——所以格数要和"含行标签列"的列数对齐，不是和内容列数对齐。新表格缺 `table_rows` 即报错（legacy 只读）。`has_structural_context()` 同步改为按 `table_rows` 里的题号引用判定，不再靠 `row_labels` 的下标位置；`validate_leakage` 新增扫描固定单元格文字，避免把答案直接印在格子里。**镜像内实跑确认八种判定**：3 列且一行内两个空格→放行（新能力）；1 个内容列→仍放行（旧形态未被废）；行长度不一致→报「must be rectangular」；题号非升序 / 同一题号出现两次→报「exactly once in ascending printed order」；一格同时含 text 和 question_number→报错；空的固定文字格→报错；`question_number` 为布尔值→报错（`isinstance(True, int)` 为真，这条是刻意防的）。**两层都必须部**：前端 `QuestionLayouts.tsx`（+55/-7）与 `contracts/questions.ts`（+22/-2）改了渲染，后端改了 schema/validator/规则，只部一层会让新契约的包渲染不出来或旧渲染收到新字段。门禁：Skill 校验器基线全过（新增用例含 `a genuine three-column row may contain several question blanks`、`a table whose question cells are out of order`、`an answer printed in a fixed table cell`）、backend+web 1063 项、`tsc --noEmit` exit 0、vitest **465** 项 / 27 文件、oxlint exit 0。`services-stable` 2:30，`/healthz` 200 @ 0.90/0.72/0.65s，`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 与 `IELTS_AUDIO_BUCKET` 均在。前端 bundle 两个变（`index-bQyZ-yhU.js`、`agentcore-DTalbct_.js`；CSS `index-gv5RB1vA.css` 未变），CloudFront 均 200，旧 `index-B6AqQTiP.js` 已 302。未运行实际生成测试（由人在前端验证）。**上一轮（v32）的验收结论一并记此**：`web-1786258994626-2`（6 套）证明单一题组偏好**确实改变了行为**——6 套里 **3 套出现跨窗口组**（v31 基线是 0/9，21 个组边界无一例外落在 Q5/Q6），但仍有 3 套边界全落在中点，其中 `custom-6cf6e9b3-c2eee8dd` 的 `reservation`/`meal` 同为 form 却在 Q5/Q6 切开——除旁白中点外无理由，说明偏好生效但未彻底 |
| 2026-08-09（午 15:0x） | `single-group-20260809`（**仅 backend**；frontend 停在 `group-windows-20260809`） | **`583ecbe`** | — | **32** | 46（未动） | **题组生成偏好改成「先试一个自然题组」+ 禁止前端表达不出的多内容列表格。** 上一轮（v31）只是**放开**了跨窗口约束，实测发现放开不等于会用：改动后第一批 `web-1786256231029-1`（9 套 / 1250s / 9-9 交付）**21 个组全部单窗口，边界无一例外精确落在 Q5/Q6**，连相邻两组都是 form 的两套（`a712feab`、`8ebaae35`）也在中点硬切——模型仍把旁白当版面边界。所以规则文本从「组数不预设」改成「**组数不预设也不偏好**：先测 Q1-Q10 能否合成一个自然的 Form/Note/Table，能就用跨两窗口的单组；不能才在**可见记录结构真正改变**处切。一/二/三组都是合法结果，没有配额或目标分布，中点提示本身永远不构成拆分理由」。第二件是**表格列结构收严**：`question_package` 没有题号到单元格的映射，所以一张表只能表达「一列行标签 + 恰好一列内容」；此前 schema 允许多个 `column_labels`，模型生成两个内容列时前端渲染不出对应关系。`validate_layout_structure` 新增 `len(columns) != 1` 即报错，出题规则 / SKILL / 审核规则同轮改齐（四处都在镜像内确认到位）。**只部 Runtime**：`git diff --numstat 29a5de2..583ecbe -- frontend/` 为 0 个文件，前端渲染逻辑没动——这次是**给生成加约束以适配现有前端**，不是改前端去适配生成。**镜像内实跑确认五种表格判定**：1 个内容列→放行；2 个内容列（`Requirement`+`Notes`）→报「no question-to-cell mapping」；2 列且都是真实比较轴（`Regular lesson`+`First lesson`）→同样报错（这条是刻意的，规则限制来自 schema 表达能力而非语义质量）；完全没有列标签→仍报旧的 `column_labels` 错；缺 `row_header_label`→上一轮的规则仍生效未被削弱。门禁：Skill 校验器基线全过（含两条新用例 `a table with one content column passes`、`validator catches: a table with two content columns but no cell mapping`，以及上一轮的 `one natural note group may cover Q5-Q7 across the narrator midpoint`）、backend+web 1063 项。Runtime READY，`/healthz` 200 @ 0.92/0.65/0.70s，web 层确认未被触碰（taskdef 46、running 1/1、单 deployment，线上 bundle 仍是 `index-B6AqQTiP.js` / `agentcore-B_tDHizk.js`）。未运行实际生成测试（由人在前端验证）。**待观察**：单一题组偏好是**规则文本**而非校验器检查，镜像内只能确认文本到位，真实效果必须看下一批的组形态——判据是跨窗口组出现率从 0/21 上升，以及不再出现「相邻两组同 layout 却在 Q5/Q6 切开」 |
| 2026-08-09（午 14:1x） | `group-windows-20260809`（backend + frontend 同 tag） | **`29a5de2`** | — | **31** | **46** | **页面题组与旁白证据窗口解耦。** 旁白窗口原本被当成两重边界：既约束每道题的证据落在哪一段，又强制一个印出来的 form/note/table 不许跨过中点提示。后者是过度约束——真实试卷里一张连续的表格完全可以跨过「now look at questions 6-10」，而旁白是听与读的节奏提示，不是版面切割线。改动后：**每道题证据的窗口仍然严格**（`window_of(number)`、declared turn 落点、blueprint item 三方一致，AL-017/SC-019 无容差），但**组不再因跨窗口被拒**。`narrator_window_id` 从 group 的 `required` 里移除，降级为「整组落在单一窗口时的兼容字段」——跨窗口的组必须**省略**它，因为没有哪个标量值是真的。QR-026 的 signpost 计数改为按组实际覆盖的窗口逐个要求，一条不能同时算给两个窗口。材料侧 `validate_part1.py` 同步删掉约束 5（`form_group` 跨窗口即错），非连续编号（约束 3）与证据序连续性（约束 4）保留。`question_metrics._groups` 新增权威字段 `windows`（组实际占据的窗口列表），旧的标量 `window` 保留给旧消费者。前端 `questionPreview` / `QuestionPreviewPanel` 按 `windows` 渲染跨窗口题组。**镜像内实跑确认九种判定**：跨窗口+省略→放行；跨窗口+仍声明→报「必须省略」；单窗口+声明正确→放行；单窗口+声明错窗口→仍报错（旧检查未被削弱）；单窗口+省略→放行；schema 的 `group.required` 已不含 `narrator_window_id`；材料侧跨窗口 form_group→0 错、非连续编号→仍 1 错；`question_metrics` 输出 `windows` 字段。**这是 8/8 以来第一次工作树干净的发布**（只有一个未跟踪的 `material/` 目录），所以 `29a5de2` 真能复原镜像。门禁：backend+web 1063 项、Skill 校验器基线全过、`tsc --noEmit` exit 0、vitest 462 项 / 27 文件、oxlint exit 0。前端三个 bundle 有两个变（`index-B6AqQTiP.js`、`agentcore-B_tDHizk.js`；`index-gv5RB1vA.css` 未变，因为本轮没动样式），CloudFront 均 200，旧 `index-CEW6rYx4.js` 已 302。`services-stable` 2:47，`/healthz` 200 @ 0.91/0.70/0.62s，`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 与 `IELTS_AUDIO_BUCKET` 均在。未运行实际生成测试（由人在前端验证）。**基线对照**：部署前用旧镜像（v30）跑完的 `web-1786247321688-1`（9 套）9/9 交付、1634s，每套都是 2 个组且各自锁在单一窗口——正是本轮要解耦的形态，可作为改动前后的对比样本 |
| 2026-08-09（午 12:1x） | `comments-20260809`（**仅 frontend**；backend 停在 `carrier-parens-20260809`） | `f3c8bc9` | — | 30（未动） | **45** | **单人批注功能上线，只部 web 层。** 新增批注的增删、严重程度（critical/major/minor）、题目侧栏、Turn 折叠面板、定位跳转，并落 S3 持久化。后端新增 `web/comment_store.py`（`CommentService` + `S3CommentStore`/`InMemoryCommentStore`，last-write-wins，一材料一份 JSON 存在 `_comments/<material_id>.json`）与三条路由 `GET/POST /api/material-comments/{material_id}`、`DELETE /api/material-comments/{material_id}/{comment_id}`。**Runtime 刻意未动**（改动全在 web 层，出题/材料生成逻辑零变更），所以这是台账里第一次两层不同版，回退方式见「当前生产版本」的第一条 ⚠️。**IAM 无需改**：task role `ielts-part1-web-task` 的内联策略已按 `arn:aws:s3:::ielts-part1-materials-<aws-account-id>/*` 授 `s3:GetObject`/`s3:PutObject`，覆盖新的 `_comments/` 前缀。三条新路由自动受 `ApiAuthMiddleware` 会话保护（`PUBLIC_API_PATHS` 未加它们）。**验证要点**：taskdef 45 带着 `IELTS_AUDIO_BUCKET`——`build_comment_store()` 在这个变量为空时会静默退回 `InMemoryCommentStore`，那样批注会随任务重启消失，所以这个变量是本轮的关键配置而非可选项；`ALLOWED_EMAIL_DOMAINS=amazon.com,example.com` 已显式带上。路由存在性用**镜像内枚举 `app.routes`** 确认（三条路径与方法齐全），不能靠线上探针推断：`ApiAuthMiddleware` 在路由之前跑，任何未认证的 `/api/*` 都返回 401，连不存在的路径也是 401。另外**带 body 的 GET 会被 CloudFront 挡成 403 HTML**，不是应用行为——干净的 GET 返回应用自己的 JSON 401。前端三个 bundle 哈希全变（`index-CEW6rYx4.js`、`agentcore-B36tGOgS.js`、`index-gv5RB1vA.css`，均 CloudFront 200；旧 `index-DCdFAgHO.js` 已 302），这与「本轮真有前端代码改动」一致。门禁：web 313 项、backend+web 1062 项、`tsc --noEmit`、vitest 461 项 / 27 文件、oxlint 全过；`services-stable` 2:38，`/healthz` 200 @ 0.95/0.64/0.70s；`_comments/` 部署前后均为 0 对象（等人工产生第一条批注） |
| 2026-08-09（晨 10:5x） | `carrier-parens-20260809`（backend + frontend 同 tag） | `f3c8bc9` | — | **30** | **44** | **括号与空 carrier 规则修复。** 有行标签的 form 行（或行列标签齐备的 table 单元格）里，两侧 carrier 同时为空是**正常且正确**的——标签已经做了命名这件事。此前的分类器把这种「带标签的裸字段」按 carrier 为空算成 medial，逼着出题去编 `(as spelt)` 这类填充文字；现在 `has_structural_context()` 判定考生可见标签是否已识别该空格，是则归 `final`，否则仍按 QR-026 报「孤立空格」。括号本身不再是问题，只有四道检查（消除真实歧义、与录音/记录格式一致、非冗余、不泄漏）全过才允许；`(as spelt)`/`(as mentioned)` 这类针对作答行为的评论归入自然度发现。审核规则第 12 项同时明确「**问题是重复本身，不是措辞**」——修法是清空或改写 carrier，不是换同义词。schema 的 `carrier_before` 描述、出题规则、审核规则第 7/12 项四处同轮改齐。**镜像内实跑确认**了六种组合的判定（有标签+空 carrier→final 不报错；无行标签、或 table 缺列标签、或 note 无标签→仍报孤立空格；carrier 存在时分类逻辑不变）。预算四值未受影响（3600/300/3300/420 与 3300/3450 均已复核）。前端 bundle 哈希未变（本轮只改后端 Skill），按同一 tag 重建两层以保持台账与 `rollback.sh --to <tag>` 的一键回退语义 |
| 2026-08-08（夜 23:2x） | `stream-budget-20260808`（backend + frontend 同 tag） | `f3c8bc9` | — | **29** | **43** | **预算改按流式上限算，不再按同步上限。** `generate_sets` 走 SSE，适用 AgentCore 的 60 分钟流式上限，而内部四个值一直按同步的 900s 设：Runtime `IELTS_HARD_LIMIT` 900→**3600**、`IELTS_SAFETY_MARGIN` 90→**300**（工作窗口 810→**3300s**），web 每套墙钟 `WEB_PER_MATERIAL_WALL` 900→**3300**，`WEB_RUNTIME_READ_TIMEOUT` 900→**3450**（夹在 3300 与 3600 之间：低于后端窗口会让 web 先于平台放弃，高于 3600 则等一个已被平台关掉的流）。这正是 `web-1786200345304-1` 只交付 3/6 的根因——三个卡位材料都写完了，却被 `may_start_questions()` 以「剩余不足 420s」挡在出题阶段外，而整批只用掉 534s / 810s。`QUESTION_P95_SECONDS` **保留 420**：它作为阶段启动保护在 55 分钟窗口下仍然合理，而这一批实测出题阶段 280/291/324s（各含 2 轮修改），420 有余量。四个值线上都没有环境变量覆盖，已逐一在两个镜像内部实跑确认。前端 bundle 哈希未变（本轮只改 Python），同 tag 重建保持两层一致。**验收（`web-1786205364950-1`，6 套）：1121s 交付 6/6（`counts={succeeded:6, failed:0, skipped:0, degraded:0}`，六个子请求 `request.json` 全部 `status=succeeded`、`resumable_slots` 为空），`time_budget` 断点 0 次。** 关键证据是**两个卡位在 900s 之后才完成**——slot-6 于 t+941s、slot-4 于 t+1121s 正常 `complete` 并写出 `_questions/`；同步上限若适用，这两次 invocation 会在 t+900 前后被平台掐掉且不通知客户端（见「AgentCore invoke 时限」），它们不可能写出完整题目包。这坐实了 `generate_sets` 适用 60 分钟流式上限，也坐实了 810s 内部窗口就是上一批断点的根因。三个困难卡位这次都是把新预算花在重试上并成功（slot-4 出题崩溃→重启→判不可交付→换候选→通过；slot-5 换候选 1 次；slot-6 重启 1 次），旧窗口下无一能走完。限流 0 条，`/healthz` 全程 0.875–0.918s |
| 2026-08-08（夜 22:4x） | `row-header-20260808`（backend + frontend 同 tag） | `f3c8bc9` | — | **28** | **42** | table 的左上角格改由 `structure.row_header_label` 命名，不再永远留空：`column_labels` 只管右侧内容列，行标签列自己那一列也需要一个名字，否则考生读不出行标签在比什么。schema / 校验器（table 组缺它即报错）/ 出题规则 §4 / 审核规则第 13 项 / 前端 `TableLayout` 五处同轮改齐。**已知副作用**：`20260808-bike-hire-*` 这类旧 table 包现在校验不过（与 note 那条同性质，都是新契约对旧包的回溯要求） |
| 2026-08-08（夜 22:0x） | `rubric-gate-20260808`（backend + frontend 同 tag） | `f3c8bc9` | — | **27** | **41** | **蓝图阶段挡住装不进任何 rubric 的 target**：六条标准 rubric 里最宽的也只允许三词 + 一个裸数字，所以 `9 and 1`（1 词 + 2 数字）无解——出题阶段既不能换 target 也不能改 Script，只能空转到断点（实测 `web-1786195813332-1` 烧完 810s 交付 0 套）。`validate_part1.py` 新增 `WORD_LIMITS`/`budget_of`/`fits_any_rubric`，出题校验器删掉重复实现改为 import（同一套算术，避免两个真相源）；SKILL.md + specification.md 写入这条给出题人的规则。落在 preflight gate 2，所以会先进 3 次重生成反馈、仍不行才换候选材料。前端 bundle 哈希未变（本轮只改后端 Skill），同 tag 重建保持两层一致 |
| 2026-08-08（夜 21:2x） | `note-sections-20260808`（backend + frontend 同 tag） | `f3c8bc9` | — | 26 | 40 | note 版式改用 `structure.note_sections[]`（`heading` + `question_numbers`），校验器要求对该组题目形成精确且不重叠的覆盖，`hierarchy` 降级为旧包只读字段；前端按声明渲染，旧包退化成无标题列表而不按下标硬配。**已知副作用**：`20260808-accommodation-rental-b5cfe833` 等携带 legacy `hierarchy` 的旧包现在校验不过（前端仍能安全降级显示） |
| 2026-08-08（夜 20:5x） | `carrier-style-20260808`（backend + frontend 同 tag） | `f3c8bc9` | — | 25 | 39 | Form 空格不再粘连成 `record1`/`of3`（去掉 `.qp-blank` 的 `margin`，按标点决定是否补空格）；note 标题与对应题目紧邻；signpost 移入审核区不给考生看；Skill 禁止命令式 Form carrier；保留合理的括号单位提示 |
| 2026-08-08（夜 20:0x） | `page-shape-20260808`（backend + frontend 同 tag） | `f3c8bc9` | — | 24 | 38 | 题面按真实试卷排版：candidate face 不再显示 Form/Table/Note badge 和旁白窗口编号，内部信息移入独立 `.qp-audit` 审核区；修 Questions / instruction / title 的间距与层级；出题规则细化（label 与 carrier 各司其职、按自然结构选 layout、无真实比较轴不得用 Table、signpost 必须具体且 Script-grounded、Instructions 用标准 IELTS 固定措辞、Note 默认最多两层）。**taskdef 从 35 跳到 38**：36（`announce-delivered-20260808`）、37（`question-restart-20260808`）是此前已注册但未记账的两次 |
| 2026-08-08（晚） | `anchor-fix-20260808`（backend + frontend 同 tag） | `12f3c30` | — | **21** | **35** | quote 落在相邻 ±1 turn 且能唯一逐字定位时归一化为 agreement 并留痕；引用邻句仍硬阻断；生成器与 validator 双侧要求 quote 与 `turn_index` 一致；agreement 不足若只是已列 adjacency 的算术则不重复加 blocker。**顺带把 taskdef 34 静默变回 `*` 的 `ALLOWED_EMAIL_DOMAINS` 收回 `amazon.com,example.com`**。前端无改动但同轮重建，保持两层标签一致。验收范围仅健康检查 + 接口版本，一套真实 `generate_sets` 由人从前端手动发起 |
| 2026-08-08 | `gate-fix-20260808`（backend + frontend 同 tag） | `062668c` | — | 20 | 34 | 交付门不再因单个 MINOR 就拒；固化 `answer_category` 边界；修 `rollback.sh`。**此次部署没在环境里带 `ALLOWED_EMAIL_DOMAINS`，`service.sh` 的 `${ALLOWED_EMAIL_DOMAINS:-*}` 默认值把公网注册静默放开了**，次轮修回 |
| 2026-08-07（晚） | `two-states-20260801`（回退，镜像未重建） | `fa598d9` 的代码 | — | **19**（= 17 的镜像） | **32** | **回退 `prod-20260807`**：验收 6 条里 2 条硬失败，5 次真实 invocation 一套都没交付。见下方第二次演练记录 |
| 2026-08-07 | `question-sets-20260807`（backend + frontend 同 tag） | `c9b1709` | `prod-20260807` | 18（已下线） | 33（已下线） | 前端改发 `generate_sets`（题目自此真的会生成，此前 `_questions/` 一直为空）；出题的 13 个环节名与断点状态进入进度显示。两个产物同轮上线——线上旧镜像的 `web/app.py` 里 `material-questions` 出现 0 次，只部一半会让新页签调不存在的接口。**当日回退** |
| 2026-08-05 | `two-states-20260801`（未变） | `fa598d9` | `prod-20260805` | 17（未变） | **32** | 仅收紧 `ALLOWED_EMAIL_DOMAINS`：`*` → `amazon.com,example.com`，镜像未重建。同时作为回退演练素材，见下方演练记录 |
| 2026-08-01 | `two-states-20260801` | `380869a` | `prod-20260801` | 17 | 31 | 状态收到两个、撤回改为整批、候选保留期延到 30 天 |
| 2026-07-31 | `agent-autonomy` | `a3bf922`（推定） | — | 16 | ~30 | Agent 自主性重写：Python 只做调度，模型步骤改为调预定义 Agent |
| 2026-07-30 | `known-good-20260730`（backend）/ `dev`（frontend） | — | `backup/main-20260801-pre-agent-autonomy`（远端分支） | 15 | — | agent-autonomy 重写之前的最后一版 |

> 2026-07-31 及更早的行是从 ECR 推送时间与 git 提交时间对照**推定**的，不是当时记录的。
> `dev` 这个 frontend 标签是在 ECR 设为 IMMUTABLE 之前推的，曾被覆盖过，不能作为可信回退点。
> 从 `prod-20260801` 起，每行都在部署时写入。
>
> **`service.sh` 里 `ALLOWED_EMAIL_DOMAINS` 是 `"${ALLOWED_EMAIL_DOMAINS:-*}"`**：环境里不带这个
> 变量，部署就会把公网注册静默放开（2026-08-08 的 taskdef 34 就是这么丢的）。每次跑
> `service.sh` 都要显式带上 `ALLOWED_EMAIL_DOMAINS=amazon.com,example.com`；同理，按 taskdef
> 回退时要先确认目标 revision 的这个值，`--taskdef 34` 会把 `*` 一起带回来。

## 怎么补一行

```bash
# 部署后，采集三个真实值（而不是凭记忆写）
aws bedrock-agentcore-control list-agent-runtime-endpoints \
  --agent-runtime-id ielts_part1_runtime-fA4wkq8nKf \
  --query 'runtimeEndpoints[?name==`DEFAULT`].liveVersion' --output text
aws ecs describe-services --cluster ielts-part1 --services ielts-part1-web \
  --query 'services[0].taskDefinition' --output text
git rev-parse --short HEAD
```

要让某一行成为可回退锚点，给它打一个带附注的 tag 并推上去：

```bash
git tag -a prod-<日期> <commit> -m "镜像/Runtime ver/taskdef ..."
git push origin prod-<日期>
```

## 回退演练记录

回退路径没跑过就等于不存在。演练记录在这里，包含实测耗时。

### 2026-08-05 — 首次演练

**素材**：把 `ALLOWED_EMAIL_DOMAINS` 从 `*` 收紧为 `amazon.com,example.com`。这项改动本身
是待办（README §5 要求公网部署必须限制域名），同时是理想的演练素材：它只改 taskdef 里的
环境变量，不重建镜像，唯一变量就是配置本身。

现有 23 个用户分布在 `amazon.com` 和 `example.com`；`email_domain_allowed` 只在
`web/auth.py` 的 `register()` 里调用，登录不查域名，所以收紧不影响任何已注册用户登录。

**步骤与实测耗时**

| # | 动作 | 结果 |
|---|---|---|
| 1 | 基线：`/healthz` | 200；`_batches/` 34、`_candidates/` 363 |
| 2 | 基线：用 `@drill-invalid.test` 注册 | **HTTP 200 通过** —— 直接证实 `*` 的问题 |
| 3 | 基于 :31 注册 taskdef **:32**（只改环境变量，镜像字节相同） | revision 32 |
| 4 | `update-service` → :32，等 `services-stable` | **165s** |
| 5 | 验证收紧：域名外注册 / `@example.com` 注册 | **403** `EMAIL_DOMAIN_NOT_ALLOWED` / 200 |
| 6 | `rollback.sh --to prod-20260801 --dry-run` | 正确识别漂移 32→31，Runtime 判为无需动 |
| 7 | `rollback.sh --to prod-20260801` | **184s**，CloudFront `/healthz` 200 |
| 8 | 验证回退真的生效：域名外注册 | **恢复 200** —— 配置确实回到 :31 |
| 9 | 旧 session cookie `/api/auth/me` | **200** —— SESSION_SECRET 跨回退稳定，用户不掉线 |
| 10 | URL 与数据核对 | 域名不变；`_batches/` 34、`_candidates/` 363 未变 |
| 11 | 前滚回 :32（本轮要保留的收紧） | **181s** |
| 12 | 清理演练留下的 3 个探针账号 | users.json 26 → 23，无 admin 被删 |

**结论**

- 回退路径**双向可证**：不只验证了「回退后健康」，还验证了「被回退的那项配置真的消失了」。
  只看 `/healthz` 200 无法区分「回退成功」和「什么都没发生」。
- Web 层单层回退约 **3 分钟**。README 之前写的 `service.sh <known-good-tag>` 回退法是假的：
  `service.sh` 无条件 `docker build && docker push`，而 repo 是 IMMUTABLE，推已存在的 tag 必然
  失败。演练当天本机 Docker daemon 根本没运行，`rollback.sh` 不受影响 —— 这正是不依赖构建的价值。
- CloudFront 域名、ALB、S3 全程未被触碰；已登录用户不掉线。
- 未覆盖：**Runtime 层**没有实际切换（v17 就是目标，切了等于没切）。
  `update-agent-runtime-endpoint --agent-runtime-version` 的参数已核实存在，
  但真实切换要等下一次后端发布产生 v18 之后才能演练。这是当前唯一未经实测的分支。
  → **2026-08-07 演练了，这个分支是坏的。见下。**

### 2026-08-07 — 第二次演练（真回退，不是演习）

`prod-20260807` 验收失败后按预案回退 18/33 → 17/32。**Runtime 层第一次真正切换**，也就是
上一次留下的那个唯一未测分支。

| # | 动作 | 结果 |
|---|---|---|
| 1 | `rollback.sh --to prod-20260805 --dry-run` | 正确识别 18→17、33→32 |
| 2 | `rollback.sh --to prod-20260805` | web 层 **stable**；Runtime 层 **失败**，见下 |
| 3 | 手工 `update-agent-runtime` 重推 `two-states-20260801` | 产生 version **19**，READY |
| 4 | 核对 v19 的 `containerUri` | `ielts-part1-backend:two-states-20260801` —— 确是旧镜像 |
| 5 | 核对线上 bundle（不是只看 `/healthz`） | `generate_sets`/`resumable_slots`/`questions_started`/断点文案 **各 0 次** |
| 6 | `/healthz`、旧 session cookie | 200 / 0.85s；`/api/auth/me` **200**，用户不掉线 |
| 7 | S3 核对 | `_batches/` 285、`_candidates/` 363、`_slots/` 13 未被删改 |

**`rollback.sh` 的 Runtime 分支当时不能用**（已于同日修复，见下）。旧代码第 163 行调
`update-agent-runtime-endpoint --agent-runtime-version`，AWS 直接拒绝：

```
ConflictException: Default endpoints are managed through agent updates.
Please use the update agent operation.
```

上一次演练只核实了「这个参数存在」，没核实「对 DEFAULT 端点可用」——参数存在不等于这条路走得通，
这正是未跑过的路径等于不存在的原因。而且它是**先 web 后 Runtime**，于是失败点把生产留在
「web 已回退、Runtime 未回退」的半回退态，比两端任何一端都糟。修的时候要一起改这两件事：
Runtime 分支换成 `update-agent-runtime`（代价是版本号只增不减，见「当前生产版本」一节），
且顺序要能容忍中途失败。

第 161 行的注释写着这样做是为了「版本列表保持真实历史，不积累 v19 = 旧 v17 的条目」——
那个目标现在达不到了：唯一可用的回退手段必然产生新版本号，所以版本号不再能反推代码版本。

**修复内容（同日完成）。** 四处，都对着上面这次失败的具体形状：

1. **Runtime 分支换成 `update-agent-runtime`**，请求体由 `get-agent-runtime <目标版本>` 的整份配置
   回放生成，而不是在脚本里重列 flag。手工回退那次是抄五个 flag 过去的，任何没想到要抄的字段都会
   被静默重置成默认值；回放整份配置则连以后 API 新增的字段也一起带回去。
2. **顺序改成先 Runtime 后 web。** Runtime 是会失败的那一步，让它先跑：失败时什么都还没动，脚本直接
   退出并明说生产未变。web 步若失败，`compensate` 用开跑前录好的配置把 Runtime 推回原样——那份配置
   在动手之前就写进临时目录了，故障当时不必再赌一次 API 调用能成功。
3. **动手前预检两层。** Runtime 目标镜像和 ECS taskdef 的镜像都要求还在 ECR（被生命周期策略清掉的
   镜像照样能读出版本记录、照样接受 update，然后拉不到镜像），taskdef 还要求是 `ACTIVE`——
   `DEREGISTERED` 的 revision 读得出来也起不了任务，而 `update-service` 会先接受再几分钟后失败。
4. **结果以 `containerUri` + ECS taskdef 判定**，不看 Runtime 版本号；不一致就以 MISMATCH 退出非零。
   `/healthz` 仍然打印，但明确标注它分不清「回退成功」和「什么都没发生」。

演练当天的六条失败路径都用 `--dry-run` 实测过（不存在的版本号、不在 ECR 的镜像标签、不存在的
taskdef、两个后端选项同时给、什么都不给），每条都在改动任何东西之前拒绝退出。
