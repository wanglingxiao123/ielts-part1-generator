# 部署前基线 — 2026-08-08

部署 `062668c`（交付门 + answer_category + rollback.sh 三项修复）之前的线上状态。
**这是回滚目标**，实测于动手前，不是从台账里抄的。

| 层 | 值 |
|---|---|
| Runtime DEFAULT liveVersion | **19** |
| Runtime 镜像（v19 的 `containerUri`） | `ielts-part1-backend:two-states-20260801` |
| ECS taskdef | **ielts-part1-web:32** |
| web 容器镜像 | `ielts-part1-frontend:two-states-20260801` |
| desired / running | 1 / 1 |

注意 **v19 装的是旧镜像**：8-07 那次回退用 `update-agent-runtime` 重推
`two-states-20260801`，产生了版本号 19。版本号推不出代码版本，回滚目标只能按镜像标签认。

## 回滚命令

```bash
bash deploy/rollback.sh --runtime-image two-states-20260801 --taskdef 32 --dry-run
bash deploy/rollback.sh --runtime-image two-states-20260801 --taskdef 32
```

按镜像标签而不是 `--runtime-version 19`：两者等价（v19 记录的就是这个镜像），但镜像标签是
直接说明意图的那一个。脚本会在动手前预检两层镜像是否还在 ECR、taskdef 是否 ACTIVE，
先 Runtime 后 web，web 步失败会把 Runtime 补偿回来，结果以 `containerUri` + taskdef 核验。

## 本次部署目标

| 层 | 新值 |
|---|---|
| 镜像标签（两个 repo 同名） | `gate-fix-20260808` |
| 代码 | `062668c` on `feat/listening-full-test`（**不合并 main**） |
